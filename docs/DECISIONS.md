# 01AskBible · 决定记录

讨论中一达成共识就当场写在这里，不要攒到收尾。每条三行：决定了什么 / 为什么 / 日期。
推翻的标「已作废 + 日期 + 新决定在哪」，不要删。

## 决定

### 圣经目录书卷行不放行尾「›」
- **决定了什么**：原生 iOS / Android 的圣经目录，每一行书卷右侧的「›」箭头去掉，行内只留编号 + 书名。
- **为什么**：Josh 2026-09-11 看 iOS 截图直接指出不要这个箭头；整行本来就可点，箭头只是视觉噪音。RN 版（askbible-mobile）暂未同步，见 OPEN-ITEMS。
- **日期**：2026-09-11

### 底栏 Tab 按钮补 contentShape
- **决定了什么**：`ShellTabBar` 的四个 Tab 按钮加 `.contentShape(Rectangle())`，整块 52pt 高的区域都可点。
- **为什么**：SwiftUI 的 plain Button 只把字形笔画当命中区，图标空心处点不动，实测底栏切不了页。同 project_ios_tap_targets 记的那条坑。
- **日期**：2026-09-11

### 人物锚点像用 ChatGPT 网页版出图，不走 API
- **决定了什么**：圣经人物透明底立绘沿用《01AI 短剧》那套「内置浏览器驱动 ChatGPT 网页版 → 会话接口配对 → 页内 fetch 取原图」流程（`~/Desktop/APP/01AI 短剧/docs/CHATGPT-出图流程.md`），**不调图像 API**。风格锁复用 `lib/figures/scene-style-guide.ts` 里的 `CHARACTER_PORTRAIT_STYLE_PROMPT`。
- **为什么**：Josh 2026-09-11 明确「不是要 API 来跑」；网页版是登录态、零额外成本，且 AI 短剧线程已实跑验证批量发批量取可行。
- **日期**：2026-09-11

### 第一批只做 12 个核心人物 × 1 张成年像
- **决定了什么**：人物库 104 个 profile 里，先给 `importanceTier == "core"` 的 12 人各生成 1 张成年期透明底立绘；少年大卫、老年摩西这类多阶段等真要用时再补。
- **为什么**：Josh 2026-09-11 选的范围。量小先验证风格一致性，跑通再扩到 88 个主人物。
- **日期**：2026-09-11

### 人物立绘的出图规则（ChatGPT 会话内约定）
- **决定了什么**：只报人物名，不喂长提示词——ChatGPT 会自己扩写。会话里已立三条硬规则：每个人物必须重新设计骨相、不得沿用上一张的脸；不要人人都持道具、不要都做邀请手势；站位方向按各人身份不同。骨相备份写在 `data/figure-visual-profiles/bundle.json` 的 `facialStructureZh`，出图时不必发。
- **为什么**：Josh 2026-09-11 实测，给参考图会把脸整个抄过去；长提示词反而不如让 ChatGPT 自己扩写。
- **日期**：2026-09-11

### 人物 slug 统一到人物库
- **决定了什么**：`data/figure-visual-profiles/bundle.json` 与 `data/scripture/scene-illustrations/GEN-33.json` 的 slug 改用人物库口径（`jacob-patriarch` / `figure-esau` / `figure-leah` / `figure-rachel` / `joseph-genesis`），参考图文件名同步改名。
- **为什么**：原来视觉库用 `jacob`、人物库用 `jacob-patriarch`，两套对不上，以后按 slug 关联会断。
- **日期**：2026-09-11

### 人物立绘定版为「圣经人物博物馆系列」标准
- **决定了什么**：人物立绘的风格、构图、透明底、眼神、动作、骨相、服饰七条规则整理成一份标准，存 `docs/figure-portrait-standard.md`，含可直接复用的底层模板。以后出图只报人物名 + 时期。
- **为什么**：Josh 2026-09-11 在 ChatGPT 会话里逐轮调教后定版，避免每次重新讨论风格、避免所有人一张脸一个姿势。
- **日期**：2026-09-11

### 省积分写进开发规则
- **决定了什么**：`~/Desktop/APP/CLAUDE.md` 第六节扩成三块——日常习惯、按单价排序的替代做法表、必须主动提醒的时机清单。对所有项目生效。
- **为什么**：Josh 2026-09-11「减少烧积分也要定入开发，及时提醒」。这轮出图早期用截图轮询进度白烧了一大截，换成查 DOM 状态标志后每轮只剩几十 token。
- **日期**：2026-09-11

### 出图改为每条都带完整风格前缀
- **决定了什么**：不再只报人物名。每一条都发 Josh 定版的「统一系列风格」整段前缀，末尾加人物名 + 时期。前缀逐字存在 `docs/figure-portrait-standard.md` 末节。
- **为什么**：只报名字时会话被前面的图带偏——风格漂成真人写实，透明底变成画上去的棋盘格。前缀把这两个坑写死了。这条推翻了本文件上面「只报人物名」那条。
- **日期**：2026-09-11

### iOS 真机安装改用 CLI，不再开 Xcode GUI

- **决定了什么**：安装新版到 iPhone 全程用 `xcodebuild` + `xcrun devicectl` CLI 完成，不需要打开 Xcode GUI。跟 Claude 说「装到 home 手机」或「装到 David 手机」即可，自动编译并安装。
- **为什么**：之前每次要手动打开 Xcode、选设备、点 Run；CLI 更快且可自动化。根本原因是 `DEVELOPMENT_TEAM = AJ2998VZH6` 之前只存在 Xcode 用户缓存里（不进 git），今天已写入 `apps/askbible-ios/AskBible.xcodeproj/project.pbxproj`，CLI 和 GUI 都能直接读到。
- **不会被清理掉的理由**：清理脚本只扫 `~/Desktop/APP` 下的 `dist/build/.next/DerivedData` 等目录，不碰 `~/Library/`（证书、Provisioning Profiles 在这里）；pbxproj 已提交进 git，不会丢。
- **每次安装必须先卸载**：覆盖安装会导致容器状态脏，App 启动 0.4 秒被 SIGKILL，无崩溃报告。流程固定为：卸载 → 编译 → 安装。卸载会清本地登录，需重新登录一次。
- **设备 UDID**：home 手机 `00008101-001641020C98001E`，David iPhone `00008120-0002598A0244C01E`。
- **日期**：2026-09-13

### Android 包体压缩：音乐 + info-edition + 自然场景视频改为按需下载

- **决定了什么**：三类大文件不再随安装包内置，全部改为按需从 R2 流播或下载：
  1. **音乐 MP3（68MB）**：5 首 starter tracks 删出 assets，MusicCatalog 所有 164 首 `bundled: false`，ExoPlayer 走 R2 HTTPS 流播。
  2. **info-edition.sqlite（26MB）**：上传到 R2 `bible/info-edition.sqlite`，新增 `InfoEditionDownloader`，首次点击「陪你探索 / 查找资料」时异步下载，UI 显示「首次加载中」进度条，下载完成自动显示内容。
  3. **自然场景视频（9.7MB）**：只保留默认景（雪山湖）内置，其余 8 景走 R2 HTTPS 流播（ExoPlayer / AVPlayer 原生支持）。
- **为什么**：Josh 确认。Android 安装包从约 92MB 压到约 24MB（-68MB）。
- **日期**：2026-09-13

### Solid Joys 不自己翻译、不自己配音，也不抓站
- **决定了什么**：在 Desiring God 另行书面同意之前，Solid Joys 只用八福伙伴现成的中文译文和音频。不自行翻译、不用 TTS 生成朗读，也不去八福伙伴站点或公众号批量抓取。
- **为什么**：DG 2026-09-10 回复明确写了不授权新译和新录音，TTS 朗读同样算新录音。八福伙伴官网本身没有每日灵修正文，内容只在微信公众号和福源课堂。详见 `docs/content-permissions.md`。
- **日期**：2026-09-14

### 再给 Desiring God 回一封信，三件事合并成一封
- **决定了什么**：在原邮件串里回复，一次问三件事：请 DG 提供或牵线取得八福伙伴中文版文件并确认授权是否已覆盖；请求重新考虑授权翻译现行版；新申请 John Piper 讲道的中文翻译配音。草稿由 Claude 起草，Josh 自己从 Gmail 发。
- **为什么**：八福伙伴没有邮箱、官网打不开，找 DG 最省事；新译请求刚被拒过，所以写成附带审核条件的重新考虑请求，而不是直接要英文文本。
- **日期**：2026-09-14

## 已作废

### 划重点灵敏度修复（2026-09-13）
- **决定**：修复划重点 pan 手势漏字问题，不删除功能。
- **为什么**：根因是 `paintAt` 每次只标当前触点的 1 个字符，手指快划时两次回调之间的字符被跳过。基础设施（TextKit 绘制、存储、会员同步、HighlightBar UI）已全齐，修复成本低（约 20 行），删除浪费。
- **修法（最终 Fix 5）**：去掉 pan gesture，改用 `FlowTextView.touchesBegan/Moved/Ended` 直接接触摸。`prevPaintIdx` 填补连续区间；`pendingPaints`+`liveHighlightRuns` 在手势结束前仅在本地绘制预览，结束时批量提交 `onPaint`（消除每字符 SwiftUI 刷新的卡顿）；`setParentScrollEnabled` 同时设 `delaysContentTouches = false`，阻止 ScrollView 抢触摸。
- **验证**：2026-09-13 安装真机验收通过，丝滑连续。
- **日期**：2026-09-13

### 划重点两个 bug 修复（2026-09-13 真机复验通过）

**Bug 1：黄色落不下来（persistence 断链）**
- **根因**：加节号高亮支持时把 `textStarts = built.textRanges` 改成了 `built.ranges`（含节号），导致 `paintAt` 里算出的字符下标与 `VerseHighlightStore` 存储格式错位，`runs()` 找不到对应区间，`highlightRuns` 始终为空。
- **修法**：`textStarts = built.textRanges`，`textOffsets = [:]`，回到 Fix 5 坐标映射。节号视觉高亮靠 `runs()` 里 `runStart == 0 → fullStart` 的扩展逻辑处理，不需要在 `paintAt` 里覆盖节号区。
- **日期**：2026-09-13

**Bug 2：下部分选不了（touch blocking）**
- **根因**：`ParchmentCardModifier` 的 background 内部有一张 `UIScreen.main.bounds.height` 高的羊皮纸图，该背景视图会截获 SwiftUI 触点，使底部 ~8 行文字区域的触点无法到达 UIKit 层的 `PaintGestureRecognizer`。
- **修法**：`ParchmentCardModifier` 加 `backgroundHitTesting: Bool = true` 参数；HighlightBar 传 `backgroundHitTesting: false`，背景 `.allowsHitTesting(false)` 穿透给下层 FlowTextView；颜色按钮 / 完成按钮作为前景元素不受影响，仍可点击。
- **日期**：2026-09-13

## 划重点同步改为整份「后改的为准」（2026-09-14，安卓原生）
- **决定**：`highlights` blob 不再逐字取并集，整份以 updatedAt 新的一侧为准；updatedAt 用本机最后一次划/擦的时间，擦空也上传；本机有未同步的擦除时算「有进度」，不走只拉云端。
- **为什么**：并集下擦掉的字会被云端旧副本补回（Josh 反馈清除后自动恢复）。代价：两台设备同时离线各自划，后同步的一方覆盖先前的。

## 原生安卓上 Play 沿用 me.askbible，顶替 RN 版（2026-09-14）
- **决定**：原生安卓 applicationId 改为 `me.askbible`，作为 Play 现有条目的更新发布，不新建条目。
- **为什么**：Josh 定：Play 原来就是 me.askbible，保留现有用户与评价，不重新做一个包。

## RN→原生安卓：本机数据一次性迁移（2026-09-14）
- **决定**：原生首次启动读 RN 残留的 `databases/RKStorage`，把读经进度类键拼成会员同步 blobs 走 `applyBlobs`；只跑一次（失败也不重试），不删 RKStorage；登录态与界面设置不迁。
- **为什么**：RN 存盘 JSON 与同步 blob value 同形，复用现成解析最省；登录 token 体系不同迁不了，数据有云同步兜底。先迁移再推正式，避免游客覆盖更新丢数据。

## iOS 26 视觉重做：Paper = Scripture，Glass = Interface（2026-09-15）
- **决定**：iOS 原生端走「Direction 1 Glass as Instrument 主架构 + Direction 2 的羊皮材质处理」。铁律一条：**玻璃只属于操作，不属于内容**——读的时候看到的是纸，操作的时候触碰的是玻璃。
- **羊皮卷底纹保留**（Josh 2026-09-15 明确要求）：`parchment.jpg` 整图铺底的做法不动，只在它上面加极轻的边缘晕暗与暖调不均，让它从「UI 皮肤」升级成真材质；**不给羊皮页面加玻璃、不用玻璃替掉纸**。玻璃控件浮在纸之上，纸从控件后面连续延伸过去，不出现硬切边。
- **首页金句废掉双层黑色投影**：改成极轻的局部暗化（渐变遮罩），金句仍属于场景，不做成玻璃卡片。
- **底栏 + 读经坞合成一块浮起的玻璃**：不再是「两个各自画的矩形 + 6pt 缝 + 透明底栏」。
- **兼容**：一套设计两套 renderer。新增语义组件层（`AskGlass` 系列），页面里不直接写 `.glassEffect`；iOS 26+ 走 `glassEffect` / `GlassEffectContainer`，iOS 17–25 走 `.ultraThinMaterial` + hairline 描边，尺寸圆角层级交互完全一致。deployment target 保持 17.0，不抬。
- **为什么**：现有层次感 100% 靠 drop shadow + 描边硬造，视频背景上白字阴影发脏、透明底栏没有自己的视觉层；而全原生（Direction 3）会让羊皮 + 琥珀 + 自然场景这套已成形的品牌资产消失。分工清楚比"给羊皮加点玻璃"高级。
- **日期**：2026-09-15

## iOS 底栏交给系统：原生 TabView + SF Symbols + bottom accessory（2026-09-15）
- **决定**（Josh「3 可以的」，批准了原先挂着的三条）：
  1. 自绘 5 位底栏下线，改**原生 `TabView`** —— iOS 26 下就是系统 Liquid Glass Tab Bar（滚动收拢、选中形变、安全区由系统给）；
  2. 图标从 36pt MaterialIcons 字形换成 **SF Symbols + 系统标准尺寸**，并补上三语文字标签（首页/音乐/读经/圣经/探索）。**iOS 端的图标语言由此与 RN / 安卓 / 网页分叉**，这是明确接受的代价；
  3. 中央 60pt FAB 不再是 FAB，**降为第三个普通 Tab**；切到它时仍打开今日读经（原来点 FAB 的行为，现由 `onEnterPlan` 承接）。
- **读经坞挂进 `tabViewBottomAccessory`**：不再是自己在 ZStack 里浮的一块。accessory 只有一行高，所以坞分两档——紧凑档（播放 / 进度 / 下一章）挂在栏上方，点中间那条弹出完整 transport（语速 / 循环 / 搜索）的矮 sheet。一套控件两个尺寸，不写两份。
- **选中色用琥珀 `#D97707` 而不是品牌黄 `#FFB101`**：系统栏是浅玻璃，黄在上面对比不够。
- **抽屉改成浮在内容上的玻璃面板**（圆角 `AskCorner.sheet` + overlay 档影子），不再是推开内容的羊皮/纯色板。
- **为什么**：自绘栏的视觉行为离原生太远（GPT 评审第 3 条），而底栏是每一屏都在的东西；交给系统后，Liquid Glass 的收拢与形变、accessory 的联动全部免费拿到，我们只维护内容。
- **代价与回退**：iOS 17–25 拿到的是系统旧样式的栏 + 坞铺在栏上方（`AskTabShell` 里一个 `#unavailable` 分支），不是两套设计。真要回退到自绘栏，只需改回 `ShellTabBar`（已删，从 git 历史取）。
- **日期**：2026-09-15

## iOS 第三轮的三条取舍（2026-09-16，Josh「全部你安排」）
- **底栏保留文字标签**：原生 TabView 的默认行为，无障碍更好，iOS 26 滚动时系统自己会收拢。纯图标要绕开系统默认，不值得。
- **读经坞的展开保持「点中间弹 sheet」**，不追原生 accessory 上拉手势：现在这套能用、可发现性更好；等哪天 Apple 的上拉行为更成熟再换。
- **安卓 / 网页暂不跟着换图标语言**：等 iOS 这套定型，再一次性搬过去，避免来回改两遍。
- **工具收成一颗 ⋯**：章页 / 目录页右栏 5–6 颗常驻玻璃钮会挡住经文与卷名，收起来只留一颗，点开才展成玻璃簇。原则是「经文是主角，工具不常驻」。
- **划重点条不改玻璃**：唯一与下层 UIKit 画笔手势共存的浮层，模拟器合成拖拽验证不出真实画笔行为，没验证过不动。等 Josh 在真机上划两笔确认。
- **`backgroundExtensionEffect` 评估后不做**：本 App 的纸与视频本来就整屏铺满、延续到系统 UI 之后，加了看不出差别；将来上 iPad 分栏再说。
- **日期**：2026-09-16

## 读经坞：自己铺玻璃卡，不用系统 accessory（2026-09-16）
- **决定**：坞按 Josh 圈定的参考图做成两行迷你播放器（封面 + 章名 + 进度 + 倒计时 + 琥珀大播放键 + 语速/循环/下一章/搜索），用 `safeAreaInset` 铺在原生底栏上方，**不走 `tabViewBottomAccessory`**。
- **为什么**：系统 accessory 只给约 48pt 一行，两行布局会被裁（播放键与进度行都被切）。自己铺之后 26 与 17–25 同一条路，不用写两份布局；系统栏仍是原生的。
- **顺带**：章页底部渐隐从 150/104 收到 44/0 —— 150 那版是照自绘底栏调的，换原生栏后安全区已由系统计入，留那么宽就是白挡正文（Josh 真机上一眼看出）。
- **日期**：2026-09-16

## 玻璃不着色，要系统原样渲染（2026-09-16）
- **决定**：`AskGlassSurface` 在 iOS 26 上**不再给玻璃加 tint**。照片 / 视频上用 `Glass.clear`（更透，能看见背景在玻璃里折射），羊皮纸上用 `Glass.regular`（纸对比低，需要系统那层自适应提亮把控件托起来）。玻璃上白图标的可读性改由图标自己那层极轻阴影 `askGlassIconShadow()` 负责。
- **为什么**：Josh 2026-09-16 真机反馈「这些不是我们效果图里的玻璃的样子，我还是要原生的」。之前为压住白图标着的那层色（暗 0.18 / 浅 0.12）恰好挡住了 Liquid Glass 自己的折射与边缘高光，看着就是一块灰塑料。**着色是在跟系统抢渲染，不能这么干。**
- **划重点条改玻璃**：Josh 真机确认画笔（划过上色 / 擦除）正常，所以它也从纸改成玻璃，同时去掉 `backgroundHitTesting: false` 那个补丁（玻璃背景不像原来那张整屏羊皮图会吃触点）。
- **坞上保留 `1x` 与搜索**（Josh：需要留这两个）；封面暂用自然场景缩略图（Josh：暂时这样）。
- **日期**：2026-09-16

## 玻璃要有东西可折射：内容滚到玻璃后面，不再在坞上方淡掉（2026-09-16）
- **决定**：章页底部渐隐改为 **0**（Tab 页留 60 软边），内容照常滚到玻璃坞与系统底栏后面。滚动长度仍由 `safeAreaInset` 保证，最后一行能完整滚出来。
- **为什么**：Josh 连续两轮反馈「不是原生的，是一个底色，而不是玻璃」。**材质本来就是原生的**（真机 iOS 26.6.2，走 `glassEffect`，无 tint），问题是**玻璃背后没东西可折射** —— 之前把经文在坞上方就淡掉了，玻璃底下只剩一片平整的纸；真玻璃盖在平色上，看起来必然是一块底色。效果图里文字是穿到玻璃后面去的，那层弯折与模糊才是「玻璃感」的来源。
- **顺带按效果图对齐坞的几何**：封面 40、标题 13、时间 11、进度条 2、播放键 40（原 48）、循环 19、「下一章」补两行小字标签（效果图的 Next Chapter，英文界面两行、中文一行）。
- **一般规律**：Liquid Glass 的观感 = 材质 × 背后内容。评估玻璃效果时要看**有内容穿过它**的状态，静态空白处看不出区别。
- **日期**：2026-09-16

## 玻璃一律用 `.clear`，并内建「降低透明度」诊断（2026-09-16）
- **决定**：`AskGlassSurface` 在 iOS 26 上一律用 `Glass.clear`，不再按 tone 分 `.regular` / `.clear`。前景（坞标题字重 bold、时间 semibold）自己补对比。
- **为什么**：`.regular` 那档磨砂把背后压得太狠，在羊皮这种低对比底上与一块底色无异（Josh 真机连看两轮：「还是平的，看不出来」）。
- **同时内建诊断**：抽屉底部版本号后面会附 `AskGlassDiagnostics.badge` —— 辅助功能开了「降低透明度」时，系统会把**所有** Liquid Glass 换成不透明平色，表现正是「怎么调都是底色」。排查玻璃观感先看这个开关，不要去调材质参数。正常状态下这行什么都不显示。
- **日期**：2026-09-16

## 玻璃看不出效果的真正原因：滚动视图被坞挤小，内容被裁在坞上缘（2026-09-16）
- **根因**：坞原来用 `safeAreaInset(edge: .bottom)` 挂。safeAreaInset 会**压小滚动视图的可见范围**，经文被裁在坞的上边缘 —— 玻璃底下永远只有一片空白纸。真玻璃盖在平色上跟一块底色没有区别，所以前面几轮怎么调材质都没用（Josh 连问三轮「不是玻璃，是一个底色」）。
- **修法**：坞改成 `overlay(alignment: .bottom)` 浮在内容之上；滚动长度改由 **content margin** 保证（`shellBottomInset` 现在是 `contentMargins(.bottom, glassDockHeight, for: .scrollContent)`）。content margin 只加长内容、不压小滚动视图，于是经文照常从坞底下穿过去，玻璃里能看到文字被压扁弯折 —— 效果图那个质感。
- **同时关掉系统底缘渐进模糊**（`scrollEdgeEffectHidden(true, for: .bottom)`，封装成 `askNoScrollEdgeBlur()`）：系统默认在内容贴底栏时糊一层，经文先被系统糊掉，玻璃再去折射一片糊掉的东西，两层模糊叠起来也是平的。
- **触控目标回到 ≥44**：上一轮照效果图把点击框也缩了（30–38），Josh 真机反馈「图标都比较小，会误点」。**图标小是设计，触控小是 bug** —— 视觉尺寸按效果图，点击框一律 44+。
- **一般规律**：判断玻璃效果，先确认「玻璃背后确实有内容在动」。检查顺序：① 辅助功能「降低透明度」是否开着（App 的抽屉底部会报）② 内容是否被 safeAreaInset 裁掉 ③ 系统 scroll edge effect 是否把内容糊掉了 ④ 最后才是材质档位（`.clear` / `.regular`）。
- **日期**：2026-09-16

## 首页：去掉暗化层，阴影回到金句文字上（2026-09-16）
- **决定**：首页顶部 180pt 暗渐变与金句背后的径向暗化**都去掉**；金句（正文与出处）改用贴字的文字阴影 `verseTextShadow()`（紧的 0.38/r1.5 压边 + 散的 0.30/r8 托底）。
- **为什么**：Josh 2026-09-16「首页上面不要一层阴影层，而文字金句需要阴影层」。暗化层把整个上半屏的天空压暗；顶部图标现在是玻璃圆钮，自带对比，那层渐变已无用。
- **推翻**：2026-09-15「首页金句废掉双层黑影，改局部暗化」那条作废，以本条为准。新阴影比最早那版双层黑影（0.55 + 0.32）轻。
- **日期**：2026-09-16

## 读经坞第一行垫纸色底（2026-09-16）
- **决定**：坞的第一行（封面 + 章名 + 时间进度）后面垫一层 0.85 的纸色圆角底；按钮行不垫，仍透出玻璃折射。
- **为什么**：Josh 确认经文穿在玻璃后面「会」太吵。只垫文字那一小块时，背后经文会从时间两侧漏出来，所以垫整行。
- **日期**：2026-09-16

## iOS 支持深色模式（2026-09-16）
- **决定**：去掉入口的 `.preferredColorScheme(.light)`，主题跟随系统。羊皮主题改走环境值 `@Environment(\.parchment)`（由 `ParchmentSchemeInjector` 按系统深浅色注入），替掉 49 处硬写的 `Parchment.light`。
- **羊皮底纹在深色下保留**：`parchment.jpg` 以 `softLight` 0.55 叠在深底上 —— 只取纸的纹理明暗，不取亮度。
- **写死的颜色**：~57 处 `Color(rgb:)` 字面值改成 `Color(parchment:)`，深色值集中在 `Color.parchmentDarkMap` 一张表里（纸色底 → 深色表面，棕墨 → 暖浅色，绿蓝 → 提亮）。品牌黄、Google 标志色、错误红不在表里。
- **修了暗色板里一个从 RN 照抄来的错**：`Parchment.dark.humanSpeech` 原是浅色值 `#38486C`，深底上看不见，改 `#9DB2DE`。
- **澄清**：之前说「多处硬写 Parchment.light 是真正的坑」不准确 —— App 原本是**故意**锁浅色，RN 版也只用浅色（暗色板写了但从没接）。所以这是**新功能**，且 iOS 与 RN / 安卓 / 网页在这点上分叉。
- **日期**：2026-09-16

## 读经迷你坞整块垫底色玻璃、按钮行降噪（2026-09-16）
- **决定**：迷你坞整块垫纸色底（scriptureBackground 0.78），不再只垫文字行；按钮行图标降字重/改次要色、缩小，播放键两侧间距加大。
- **为什么**：Josh 真机看：经文透过按钮行太吵，GPT 效果图本来就有底色玻璃；图标太强、挨得近易误点。

## 首页图标收拢：方案 A（2026-09-16）
- **决定**：去掉右上齿轮，设置钮并到底部专辑排末尾（竖线隔开）；顶部只留一颗较小较淡的菜单钮；闲置 7 秒先收设置簇，再 7 秒淡出菜单与专辑排，只留金句 + 底栏；点空白处唤回/收起。
- **为什么**：四处图标把全景图切碎；集中到下半屏也更顺手。

## 首页去掉左上菜单、专辑排去玻璃底（2026-09-16）
- **决定**：原左上菜单（语言/译本/登录/提醒/反馈/删号）改由探索页右上「设置」齿轮打开同一面板；首页顶部不放控件。专辑排（安静/金句/下午茶｜设置）去掉玻璃胶囊，白图标加阴影直接浮在画面上。探索页抬头下的「退出登录」删掉（面板里已有）。
- **为什么**：Josh 要首页更干净，全景图完整露出。

## 欢迎页改成「开始今日灵修」，登录退出首屏（2026-09-18）
- **决定**：iOS 欢迎页不再是登录页。首屏只承担一件事：说明 App 是什么 + 一个主按钮「开始今日灵修」（进首页）。具体：
  - 语言切换从首屏的四格大卡片改成右上角轻量菜单（仍留在首屏，不藏进设置）；
  - Apple / Google / 邮箱密码 / 注册整套收进右上角「登录」拉起的 sheet，邮箱密码在 sheet 里再折叠一层（点「使用邮箱登录」才展开）；
  - sheet 里 Apple 排在 Google 之前（iOS 平台习惯，`SocialSignInButtons` 顺序改了，登录页/注册页一并受益）；
  - 「注册」降成文字链接，不再和「登录」并排等重；
  - 控件材质统一：`0xFFFCF5/0.62` → `0xF8F1E3/0.72`，输入框圆角 10 → 8（羊皮纸背景上别再堆 iOS 白胶囊）；
  - 留白重新分配：品牌落在视线上三分之一，主按钮留在拇指区，中间是呼吸区。
- **为什么**：主应用场景排序是 **灵修 → 每日读经 → 圣经目录**（Josh 2026-09-18 确认），登录只是「同步进度」的附属能力。原来首屏把欢迎页、语言选择、三种登录方式挤在一屏，第一次打开像账号墙。「灵修」在原生里就是首页（金句 + 场景 + 音频），不新建页面。
- **文案**：新增 `native.welcome*` 八条，写在 `tools/native-copy-extra.json`，由 `node tools/gen-site-copy.mjs` 生成进 `SiteCopy.swift`（SiteCopy 是生成物，别手改）。
- **怎么验证**：模拟器 AskBible-Glass 装 Debug 包，删掉容器 plist 里的 `onboardingCompleted` 键再启动，即可重看欢迎页。
- **日期**：2026-09-18

## 首页成为「今日灵修」落地页（2026-09-18）— 已作废，同日被下一条推翻
- **产品规则（这条最重要）**：**金句不是今日灵修，金句是引子；探索文章才是灵修正文。** 首页不新增内容类型，只是把「今天这一篇」提前曝光。
- **做法**：首页金句下方加一块灵修卡——「今日灵修 + 日期」小标签 / 当天那篇文章标题 / 两行摘要 / 唯一主 CTA「开始今日灵修」→ 切到探索 Tab 打开该文章（复用读经计划页打开文章那条路 `exploreArticle = a; tab = .explore`）。
- **选哪一篇**：`Home/DailyDevotion.swift`，按当天是本年第几天对可用文章数取模，同一天恒定。
- **不做成白卡片**：文字浮在风景上，底下垫一层黑 0.26→0.14 的圆角渐变保可读；风景仍是主角。
- **「今日灵修」标签归在卡里，不放金句头上**：金句照旧轮播，标签若压在轮播金句上，「今日」就成了每几秒换一次的东西。**没有采纳 ChatGPT「取消金句自动轮播」的建议**——轮播是既有体验，改它风险大于收益，靠标签归属解决语义冲突。
- **一个坑**：首页那层「点空白处收起/唤回界面」的 `Color.clear` 是整屏的，灵修卡不加 `.zIndex(1)` 的话按钮点不动（点下去只会收起界面，看起来像按钮失灵）。
- **登录主按钮统一琥珀**：`AuthSubmit` 从 `0x1C1410/0.1` 灰调改成欢迎页那颗 `0xffb101/0.22`，登录页 / 注册页一并生效。
- **日期**：2026-09-18

## 灵修就是沉浸式首页本身，不是一篇文章（2026-09-18，推翻上一条）
- **Josh 定义**：「全景没打扰的场景图，就是灵修内容本身，因为有音乐，有经文。」
- **结论**：灵修 = 不被打扰的全景场景 + 经文 + 音乐这件事本身。它不是「一篇带正文和音频的文章」，所以首页不需要任何额外的内容单元或入口——加标题、摘要、按钮恰恰是在打扰。
- **因此回滚**：当天早些时候按 ChatGPT 建议加的「今日灵修卡」（标签 + 日期 + 文章标题 + 摘要 + CTA + 遮罩）全部撤掉，首页回到场景 + 金句 + 工具排；`Home/DailyDevotion.swift` 删除，壳里的 `onOpenDevotion` 接线撤掉。
- **欢迎页的「开始今日灵修」保留**：它落到首页是成立的——进来就是灵修。
- **为什么记下来**：ChatGPT 的整套方案（金句是引子、探索文章才是正文、首页要有唯一 CTA）建立在「灵修 = 一篇文章」这个错误前提上。下个线程别再照那套推。
- **日期**：2026-09-18

## 「下午茶」和「专注」是两个东西，不改名（2026-09-18）
- **Josh 定义**：「下午茶是轻松快乐的灵修版本，而专注是另一个，是工作的。」
- **结论**：首页工具排里的「下午茶」保留原名。ChatGPT 建议把它改叫「专注」是误读——那是另一个面向工作场景的东西，两者不是一回事，合并会把一个灵修体验说成工作工具。
- **顺带**：这也再次说明灵修在这个 App 里是「氛围 + 经文 + 音乐」这一类体验，不是文档型内容（见上一条）。
- **日期**：2026-09-18

## 每日读经不做首次引导（2026-09-18）
- **决定**：不给「每日读经」加首次打开的引导（首页提示、第二天弹窗之类都不做），读经维持在底栏入口。Josh 2026-09-18 让我定，我定不加。
- **为什么**：任何引导都要往首页上贴东西，而首页的价值恰恰是不被打扰（见「灵修就是沉浸式首页本身」）。为了第二场景去破坏第一场景不划算；底栏五个 Tab 一直在，读经并没有藏起来。
- **日期**：2026-09-18

## 首页闲置：系统底栏也一起退场，常用三键留下（2026-09-18）
- **Josh 要的**：首页这块要是全景的，不要底色、更简约，甚至不要文字，因为沉浸；但音乐 / 金句朗读 / 下午茶这三颗是常用键，闲置时不要跟着消失。
- **决定**：首页闲置 7 秒后 —— 系统 Tab Bar、设置齿轮、它左边那条分隔线，全部淡出；只剩全景 + 金句 + 三颗压淡到 0.55 的常用键（底栏走后这一排往下坐到 36pt）。点屏幕任意处全部回来，再点一下立刻收起。
- **实现**：照音乐页 `musicChromeHidden` 那条现成的路——`HomeView.onChromeHidden` 回调壳里的 `homeChromeHidden`，`showTabBar` 里加一项。**没有**去改系统底栏的材质：iOS 26 的 Liquid Glass 栏不吃 `.toolbarBackground(.hidden, for: .tabBar)`（试过，玻璃胶囊底照样在），能做到「不被压着」的只有让它整个退场。
- **没有采纳 ChatGPT 的建议**：它主张底栏常驻、保留玻璃与文字，理由是导航定位与无障碍。这里的取舍是——首页的价值就是不被打扰，而「点一下就回来」的交互首页本来就有（金句以外的东西一直是这个规矩），导航并没有真的丢。文字标签也不用删了：看不见的时候，删不删都一样。
- **顺带修了两处**：① 工具簇（字号 / 定时 / 环境音）的白图标在亮天空上看不清——玻璃是 `.clear` 不着色，图标全靠自己，闲置态不透明度 0.78 → 0.95，阴影升到和专辑排同一档（黑 0.45 / r4）。② 首页那层「点空白收起/唤回」的 `Color.clear` 没有明确尺寸，不参与命中，所以底栏退场后点屏幕叫不回来；补上 `frame(width:height:)`，并把 `toggle` + `touch()` 改成先算目标状态再赋值（否则刚收起就被 `touch()` 弹回来）。
- **日期**：2026-09-18

## 成就系统：复用 MY CLASS 勋章图，三端 + 上会员同步（2026-09-18）
- **Josh 要的**：「在 MY CLASS 里有勋章图，我们也取过来，做我们的用户反馈，做一些的奖励机制，让用户更上瘾。」
- **素材复用，不重新出图**：`03MyClass/design/medals/transparent/` 的 40 枚勋章 + 66 卷书卷印章，画风本来就是基督教灵修风（油灯 / 橄榄枝 / 羊皮卷，铜银金三档），AskBible 直接用。压 512px WebP 传 `askbible-media/medals/<名>.webp`，客户端按需下载并缓存，不打进安装包。**不跑 ChatGPT 重出**——那要几十轮浏览器自动化，烧积分且收益是「两个 App 的勋章长得不一样」，不值。
- **范围**：iOS + 安卓原生 + 网页三端都做。
- **判定架构照搬 MyClass `Learning.swift`**：所有统计只吃一份 ledger，勋章 / 等级 / 进度全部**现算**，只持久化「已获得的档位 + 获得时间」。好处是补发自然发生（云端同步进来新数据，下次评估就把漏发的勋章补上），不会出现结果和数据对不上的状态。
- **数据源基本现成**：`ReadingActivityStore`（读经天集合 / 连续天 / 累计听读秒 / 使用时长 / 最近阅读）、`ReadingPlanStore`（计划完成日 / 各轨 chaptersReadKeys）、收藏 / 高亮 / 书签。
- **要新增的唯一埋点**：全局「读过哪些章」的章级 ledger。没有它算不出书卷完成、旧约 / 新约进度、66 卷印章。
- **上会员同步**：新增一个 blob 走 member-sync，换手机勋章和章级账本全带走。（MyClass 第一版只存本地，这里不照抄——AskBible 已有成熟的 member-sync 管线，顺手接上比以后补便宜。）
- **真源与生成**：勋章表写在 `data/medals.json`，由 `tools/gen-medals.mjs` 生成 Swift / Kotlin / TS 三份表，照 `gen-site-copy` 那套规矩，不手改生成物。
- **日期**：2026-09-18

## 首页设置簇去掉玻璃底（2026-09-18）
- **Josh**：「这里玻璃在浅色状态下，这样很不好看。」——晴天湖面那种亮场景下，iOS 26 的 `.clear` 玻璃会把整块提亮成一片发白的板子，风景被蒙住，连场景缩略图都被压暗。
- **决定**：设置簇（字号 / 定时 / 环境音 / 场景条）不再垫 `askGlassRect`，图标直接浮在风景上，可读性交给图标自己的阴影（黑 0.45 / r4）。这跟专辑排 2026-09-16 那次是同一个处理，首页现在没有任何一块玻璃底。
- **为什么不是换 tone 或加暗底**：加底色就又回到「一块板子压在风景上」，与首页「全景不被打扰」相悖。
- **日期**：2026-09-18

## XP 要「一直在涨」：微反馈 + 连续乘区 + 大数字（2026-09-18）
- **Josh 要的**：「需要让使用用户更有快感，会感觉到自己的 XP 一直在增加，让这个过程像游戏一样，一直有反馈的，而且进步很多，很多的 XP。」
- **三层奖励**：① **微反馈**——每读一节 +8、每听 15 秒 +12，所以读 / 听的时候进度条每几秒就跳一次，不是读完一章才动；② **里程碑**——读完一章 +250、读完一卷 +3000、计划日 +500、点亮印章 +1500，配 `+XP` 飘字和升级弹层；③ **连续乘区**——连续读经 N 天，所有 XP ×(1 + N×0.05)，封顶 2.5 倍，断了回 1.0。连读同卷还有 combo，第 2 章起每章额外 +150（封顶 +900）。
- **数字刻意做大**：连续 7 天的用户读一章（30 节）就是 ~660 XP，听 20 分钟 ~1300 XP。等级门槛前期密集（第 2 级 300 XP，读两章就到），12 级后每 60000 XP 一级。称号 12 档：启程 / 初读 / 寻道 / 勤读 / 深读 / 明光 / 通读 / 丰盛 / 根深 / 长久 / 精金 / 恒心。
- **XP 仍然是现算的**：由 ledger 推出来不单独存。这不影响手感——听读秒数和已读节数本来就在实时增长，所以 XP 自然就在实时增长；好处是云端同步进来的数据会立刻体现，不会出现「XP 和记录对不上」。
- **连续乘区是留存主力**：它让老用户的每一个动作都比新用户值钱，断签的代价肉眼可见（倍率掉回 1.0）。
- **日期**：2026-09-18

## 圣经故事对谈解读：稿子自己写，配音交给 NotebookLM（次选微软 edge-tts）

- **决定了什么**：做「圣经小故事对谈解读」时，**深度对谈稿由 Claude 自己写**；音频优先走 **NotebookLM（现已更名 Gemini Notebook）**，把写好的逐字稿当作来源喂进去、用自定义指令要求严格照稿播。首篇试点是列王纪上 13 章《无名的神人》，稿子在 `docs/story-scripts/1KI13-无名的神人.md`，喂给它的原始经文资料在同目录 `1KI13-source.txt`。
- **为什么稿子自己写**：Josh 要求**正统深度神学解读，不要心灵鸡汤**。这需要指定释经路线（圣言权威 / 申13:1-5 与加1:8 的检验 / 来自同侪的试探 / 狮子与驴作为神主权的记号 / 王下23 的应验 / 避免律法主义与廉价恩典两种误读 / 基督论收束），让 NotebookLM 自由发挥拿不到这个深度，而且它每次生成的内容都不一样、不可复现。
- **为什么音频用它**：2026-09-18 三版盲听，Josh 的结论是「NotebookLM 表现高出不少，非常自然」，明显赢过本机 Kokoro 和微软 edge-tts。首版 16:28 / 31.8MB m4a。
- **A 路（浏览器自动化）实测是通的**，不需要人工介入。可复用的步骤：内置浏览器打开 notebook.google.com（登录态常在）→「+ 新建」建笔记本 →「复制的文字」粘贴来源（**用这个，别碰文件选择器**）→ Studio →「音频概览」右侧箭头打开自定义面板 → 语言选中文（简体）、格式「深入探究」、在「AI 主持人应着重于哪些方面」里写死指令 →「立即生成」→ 等约 12-16 分钟 → 卡片右侧 ⋮ →「下载」。
  - **最后这一步「下载」必须 Josh 亲手点**：浏览器面板只允许用户自己的点击打开新标签，Claude 点会被拦下（提示「新标签页被拦截」，且确实没下下来）。点完文件落在 `~/Downloads/<音频标题>.m4a`，Claude 从那里取。前面建笔记本、粘来源、设指令、生成、等待全部可以自动，**只有这一下需要人**。
  - ⚠️ 别给页面挂 `window.open` / `HTMLAnchorElement.prototype.click` 钩子去捕获下载地址——钩子会把真正的下载也一起拦掉，连 Josh 自己点都失效，必须刷新页面才能恢复。
  - **不要试图自己 fetch 音频 URL**：地址在 `lh3.googleusercontent.com`，跨源 fetch 被 CORS 挡，内置浏览器也不允许导航到该域，`curl` 拿回来的是 Google 登录页（916KB HTML，content-type 还是 text/html）。老老实实点「下载」。
  - 转码留档：`ffmpeg -i x.m4a -ac 1 -ar 24000 -codec:a libmp3lame -b:a 48k x.mp3`（31.8MB → 5.7MB）。
- **本机 TTS 的位置**：Kokoro **弃用**（Josh 2026-09-18：「用 KOKORO 不行，以后尽量不用」）。需要本机/离线合成时默认用**微软 edge-tts**（`pip install edge-tts`，免费无需 key，52 段并发跑完不到一分钟）：晓恩＝`zh-CN-XiaoxiaoNeural` rate `-6%`，陈老师＝`zh-CN-YunyangNeural` rate `-10%`，按发言人分段生成再用 ffmpeg 插 0.6s 静音拼接。中文金句的 VoxCPM2 方案不受影响，仍按 `.claude/skills/bible-tts` 执行。
- **日期**：2026-09-18

## XP 刺激强度定 A 方案：飘字与微反馈保留，倍率只升不降，听读 XP 加三重防刷

- **决定了什么**：Josh 2026-09-18「你决定」，由我拍板 OPEN-ITEMS 第 5 条的 **A（折中）方案**，两端（iOS + 安卓）已实现：
  1. **飘字和微反馈一条不动**——每节 8 XP、每 15 秒听读 12 XP、章 250、卷 3000 的节奏保持原样，Josh 要的「XP 一直在涨」不打折。
  2. **连续天数倍率只升不降**：ledger 新增 `bestStreakDays`，倍率按 `max(当前连续天数, 历史最长)` 算，断签后不掉回 1.0。真源在 `data/medals.json` 的 `xp.streakMultiplier`。
  3. **听读 XP 三重约束**：App 在前台（`AchievementStore.foreground`，由 iOS `scenePhase` / 安卓 `ON_RESUME`/`ON_PAUSE` 驱动）＋ 确实在播 ＋ **同一章最多 80 片**（`xp.listenTicksPerChapterCap`，≈20 分钟，长章如诗篇 119 也够）。换章自动清零。
- **为什么**：ChatGPT 的评审（`docs/gamification-chatgpt-review.md`）主张把 XP 降成后台、删掉飘字和听读 XP，理由是信仰产品里外在奖励会挤出内在动机。这个担心成立的部分只有两处——**惩罚性的断签**和**挂机刷分**，都能单独修掉，不必牺牲 Josh 明确要的正反馈。所以留爽感、去负反馈、堵漏洞。
- **注意**：`bestStreakDays` 参与会员同步 JSON（取两端较大值），换设备不会把历史峰值丢掉。
- **日期**：2026-09-18

## 留存的缺口是「回归」，不是「爽感」：加 Gentle Return 卡，不加任何惩罚

- **决定了什么**：Josh 2026-09-18「你决定」。成就 / XP 三条决策已经把**在 App 里的当下反馈**做满（飘字、微反馈、勋章、印章、等级），但没有任何东西回答「离开之后为什么回来」。补一块**回归卡**：隔天回来时首页顶部出现「你回来了 / 我们上次停在这里 · <书 章>」+「接着走」，点一下直接进那一章。首次或无记录时退化成「从这里开始」。文案键已加进 `tools/native-copy-extra.json`（`native.returnGreeting` / `returnResumeTitle` / `returnResumeAction` / `returnFreshTitle`）并跑过 `gen:site-copy`。
- **为什么是这块**：「XP 刺激强度定 A 方案」把连续天数倍率改成**只升不降**，去掉了断签惩罚——这是对的（`01-vision.md` 的用户恐惧第一条就是"中断后的失败感"），但代价是**离开一周回来毫无损失，也就毫无回来的理由**。负向召回被拿掉之后，必须补一个正向的。同一个信息，「你已 5 天没来」是问责，「我们上次停在 X」是陪伴，后者才符合 `03-principles.md` 的 Gentle Return。
- **明确不做**：断签提示、"你已 N 天没读"、完成百分比、催促推送。判断标准一句话——**这个反馈在说「你做得够不够」，还是在说「你走到哪了」？前者一律不要。** 已有的 XP / 勋章属于后者（陈述累积），所以不冲突。
- **数据现成**：`ReadingActivityStore` 已有「最近阅读」和读经天集合，不需要新存储。
- **已接线（2026-09-18）**：iOS `Home/HomeView.swift` 的 `returnCard` + 安卓 `ui/HomeScreen.kt` 的 `ReturnCard`，壳层各传 `lastRead`（`ReadingActivityStore.recent.first`）和 `onResumeReading`。两处实现刻意一致：**隔天才出**（最近阅读那天 != 今天）、跟首页闲置一起淡出（7 秒）、点过本次前台不再出。底色两端都用深褐半透 `0x1c1410/0.55`，**没有用 iOS 26 的 clear 玻璃**——浅色场景下它会提亮成一块发白的板子，和同日撤掉设置簇玻璃底是同一个原因。
- **横幅让位（2026-09-18 追加）**：回归卡和勋章 / 升级横幅抢首页顶部同一个位置，Josh「你决定」。定为**有回归卡的那次启动把横幅压后**——回归卡只在隔天回来时出现，本来就稀有；升级横幅哪次都能补。实现是 `onReturnCardVisible` 报给壳，`EarnedToast` 在卡片在时不渲染且**不消费事件**。
- **全新用户照样出「从这里开始」（2026-09-18 追加）**：Josh「现在没什么用户，所以可以加的」。保留这一档，不因为「首页不被打扰」把它砍掉；等有真实用户量再回看。
- **卡片退场和底栏那条解耦（2026-09-18 追加）**：Josh「你决定」。回归卡的退场由**它自己的 7 秒计时**置位 `returnCardDone`，不只依赖 `chromeVisible`。理由：「首页闲置隐藏底栏」那条还挂在等真机确认，若退回成底栏常驻（`chromeVisible` 恒为 true），只靠它的回归卡就会被永久钉在首页顶部。安卓本来就是自带计时，这次是 iOS 对齐安卓。实测：卡片出现 → 7 秒后自己退场。
- **注意 `09-dangerous-directions.md` 已被覆盖**：那份早期文档写着"不要变成游戏化产品"，与本日三条成就 / XP 决策直接冲突，**以 DECISIONS 为准**。本线程一度按那份旧文档写过一份反游戏化提案（`docs/12-retention-feedback-proposal.md`），已删除作废。
- **日期**：2026-09-18

## 成就 / XP 账本上会员同步：新增 `achievements` blob，逐字段「只涨不退」合并

- **决定了什么**：成就账本作为**第 26 个 blob 键** `achievements` 挂进会员读经同步（`member_reading_sync_documents.blobs`），四端键表同时加：
  `lib/member-reading-sync/schema.ts`、iOS `MemberReadingSync.swift`、安卓 `core/.../MemberReadingSync.kt`（RN 那份键表不加——RN 没有成就 store，不会产出这个 blob，而合并时 base 是整份带上去的，不会丢）。
- **合并规则不用默认的「时间戳新的一侧胜」**，三端各实现一份 `mergeAchievements`，逐字段：计数（`versesRead` / `listenTicks` / `chaptersOpened` / `bonusXP` / `bestStreakDays`）取大，日期集合（`morningDates` / `nightDates`）取并集，勋章 `earned` 取更高档、同档取更早的获得时间，`chaptersRead` / `seals` 取**更早**的首次点亮时间。
- **为什么**：勋章和 XP 是单调增的荣誉，最怕「换台手机登录后勋章少了一枚」。默认的整份覆盖会让后同步的旧设备把新设备的进度顶掉；而这份账本每个字段的「正确合并」语义各不相同（计数取大、首次时间取小），只能逐字段写。三端规则必须逐字对齐，否则谁最后推谁说了算。
- **数据库不用迁移**：`blobs` 是 `jsonb`，没有键的约束（`supabase/migrations/20260617000000_member_reading_sync.sql`）。
- **空账本不推**：两端加了 `AchievementStore.hasProgress`，没有任何进度时不写这个 blob，免得新装的设备用空账本去合并云端。
- **换帐号 / 退出**：`clearLocalBlobs()` 里一并调 `achievements.clearForAccountSwitch()`——成就跟帐号走。
- **日期**：2026-09-18

## 三端对拍的「已知不一致」走白名单，不靠记忆

- **决定了什么**：`tools/member-sync-check.mjs` 加了一张 `KNOWN_DIVERGENCE` 表。三端结果不一致时，只有在这张表里写明理由的条目才放行（跑的时候打印一行 `!` 提醒），**没写在表里的一律 exit 1**。目前表里只有一条：安卓的 `highlights` 故意不用并集。
- **为什么**：这个 check 从深色模式那次提交（2026-09-16）起就编译不过，两周里没人发现三端规则有没有漂。修好之后立刻暴露出一条真实的既有不一致（iOS 擦掉的划重点会被旧副本补回，见 OPEN-ITEMS）。如果为了让它变绿就把用例删掉，等于把 check 再废一次；如果放着它红，下一个人又会习惯性忽略。白名单是唯一能同时做到「已知的不挡路」和「新出现的立刻炸」的写法。
- **配套**：`Theme/ParchmentTheme.swift` 的 `init(parchment:)` 包了 `#if canImport(UIKit)`，让 harness 能在 macOS 目标下编过；App 行为不变。
- **日期**：2026-09-18

## 网页端成就系统：直接 import `data/medals.json`，不再生成第三份表

- **决定了什么**：网页端（Next.js）落地成就 / XP 第三端。`lib/achievements/medal-catalog.ts` **直接 `import "@/data/medals.json"`**，不走 `gen:medals` 生成 TS 表；`lib/achievements/achievement-store-web.ts` 把 iOS `AchievementStore.swift` 的账本 + 判定引擎逐条移植成模块级 store（`useSyncExternalStore` 订阅）。
- **为什么不生成**：iOS / 安卓要生成是因为 Swift / Kotlin 读不了 JSON 源文件，网页端能直接 import，多一份生成物就多一份要对拍的东西。`medals.json` 只有 10KB，整份进 client chunk 无压力（相比之下探索页那份 149KB bundle 才需要挪到服务端算）。`npm run check:medals` 因此仍只对两端，网页端数值天然不会漂。
- **上报点**：`markReadChapterCompleted()`（读完一章，点亮印章的唯一入口）在模块内动态 import 调 `noteChapterRead`，所有调用方自动覆盖；`ReadChapterCompletionSection` 挂载时调 `noteChapterOpened`。
- **网页端刻意和原生不同的两处**：
  1. **一次性回填**（`backfillFromChapterCompletion`）：网页端在成就系统之前就有 `askbible-read-chapter-completion-v1` 的读完章记录，不回填的话老用户进来成就全空、印章一个不亮。用 `askbible-achievements-backfilled-v1` 作一次性标记，直接读 localStorage 而不是 import 那个模块——它反过来要调本模块，会成环。
  2. **印章在 `refreshAchievements` 里兜底补发**：原生只在 `noteChapterRead` 里点亮；网页端回填进来的历史章和云端合并进来的章都不经过那条路，所以每次评估都扫一遍「整卷读完但印章没亮」。口径没变（印章 = 整卷读完），XP 也不会重复计（`perBookCompleted` 走 `completedBookIds`，`perSeal` 走 `seals` 计数）。
- **会员同步**：`reading-sync-local-web.ts` 三处接线（`localHasMemberReadingProgressWeb` / 导出 `blobs.achievements` / `applyBlob` 的 `achievements` 分支 → `mergeRemoteAchievements`）。合并规则那半边早就写好了（`lib/member-reading-sync/merge.ts` 的 `mergeAchievements`），本轮没动。推送时机跟网页端既有做法（可见性 / focus 触发），另在 `MemberReadingSyncBridge` 里挂 `setAchievementsLocalChangeHandler` 做 1.5 秒防抖推送，对齐原生 `onLocalChange`。
- **界面**：`/explore/achievements`（`components/achievements/AchievementsView.tsx`）= 等级条 + 三个数字 + 23 枚勋章墙 + 66 卷印章墙，未点亮走 `grayscale + opacity`；探索页顶部加 `AchievementLevelCard` 等级条卡片点进去。勋章文案只有简体 + 英文，繁体运行时用 `toZhTwText` 转（同 medals.json 的约定）。
- **日期**：2026-09-19

## 网页端补齐：听读计时、即时反馈层；逐节 XP 不做，登出不清本机

- **决定了什么**（Josh 2026-09-19「你决定」，四条 OPEN-ITEMS 的处理）：
  1. **补听读计时**——网页端此前没有任何地方累加听经秒数，纯网页用户的「累计听读时长」永远是 0。在壳层播放器上按**媒体时间增量**每满 15 秒记一片，同时喂 `scriptureListenTotals` 和 `noteListenTick`。用媒体时间而不是墙钟：暂停 / 拖动 / 后台节流都不会虚增；拖进度条的大跳（≥2 秒）丢弃。
  2. **补即时反馈层**——`AchievementFeedbackLayer` 挂在 root layout，+XP 飘字 1.4 秒、勋章 / 印章 / 升级横幅 2.6 秒，依次播完。
  3. **逐节微反馈 XP 不做**——网页阅读器没有现成的逐节可见性观察，要新加 IntersectionObserver，改动面大于收益。`noteVersesRead` 留着不接线。
  4. **登出不清本机成就**——网页端 `logout` 是**故意**保留本机数据的（flush → 标记只拉不推 → 登出），26 个 blob 一视同仁。单清成就会让它成为唯一行为不同的 blob，比现状更难理解。共用电脑的账号串数据问题对所有 blob 都成立，要修就整块修，已记进 OPEN-ITEMS。
- **过程中修掉的一个既有 bug**：`writeScriptureListenTotalsWeb` 一直没有通知订阅者（`listenListeners` 建了却没人 emit），探索页的听读时长不会随播放刷新。
- **反馈层踩的坑**（值得记住）：第一版把「消费队列」写在 `setState` 的 updater 里，又用 ref 记「当前是否在展示」——React 会重复调用 updater、且会重复挂载组件，跨挂载残留的 ref 把后续事件永久堵住，表现是**只弹出第一条、后面全丢**。改成组件订阅后把 store 队列整段抽干到自己的 state 再依次展示，问题消失。
- **日期**：2026-09-19

## 网页端成就界面全面对齐 iOS：共用同一份文案真源，逐项照 SwiftUI 复刻

- **决定了什么**（Josh 2026-09-19「网页端需要对齐苹果端的效果」）：网页端的成就界面不再是「能看结果」的简化版，而是照 `Read/AchievementViews.swift` + `Read/AchievementsView.swift` 逐项复刻。
- **文案共用同一份真源**：新增 `lib/achievements/achievement-copy.ts` 直接 import `tools/native-copy-extra.json`（原生那边由 `gen:site-copy` 生成 SiteCopy）。和 `data/medals.json` 同一个思路——网页端能直接读 JSON，就不再生成第三份表，三端文案天然不会漂。繁体运行时 `toZhTwText` 转。
- **逐项对齐的清单**：
  - `MedalIcon`：未获得灰度 + 不透明度 0.28（原来是 grayscale + 0.35），获得后满色并按档位叠暖色（铜 `#B87333` / 银 `#B9BFC6` / 金 `#E8B44A`，0.45 `overlay` 混合）。网页端用 `mask-image` 把色块裁成勋章形状，等价于 SwiftUI 的「同一张图 foregroundStyle + blendMode(.overlay)」。图没到位时先占一个 `#F2E4CF` 羊皮纸圆。
  - `XPBar`：Lv 号 `#FFB101` + 称号 `#5C4030` + 火苗倍率角标（> 1.001 才出，`#E06C2A` on 12% 底）+ `xpInLevel / xpForLevel` + 渐变进度条（`#FFC94D → #FFB101`，带辉光）。原来是普通 amber 条、没有称号色、没有火苗。
  - 探索页卡片：整条可点 + 副标题「勋章、书卷印章与等级 ›」，羊皮纸底 `rgba(255,252,245,.92)` / 圆角 16 / 边 `#F2E4CF`。
  - `XPFloater`：顶部 90px，最多叠 3 条，弹入 scale 0.5 → 1，停 1.1 秒后上移 40px 淡出；`+XP ≥ perChapterRead` 用 22px 大号，否则 16px。原来是单条 1.4 秒的深色小药丸。
  - `EarnedToast`：顶部 8px、最大宽 340、羊皮纸卡 `#FFFCF5` / 圆角 16 / 琥珀描边 0.35 / 投影，2.8 秒收起，**点一下提前关**；勋章走「名 + 达成条件」，印章走「点亮书卷印章 + 卷名」，升级走「升到 N 级 + 称号」并用 Lv 圆章代替图。原来标题副标题是反的、且不可点关。
  - 成就页：表头第三个数字从「已得勋章数」改成 **连续加成 ×N**（和 iOS 一致），三数字之间加竖分隔；分节标题右侧补进度（「已获得 n / 23 枚」「已点亮 n / 66 卷」）；每枚勋章补「离下一档还差多少」的 4px 细进度条 + `current / next`，满档改显条件文案；网格改成 iOS 的自适应尺寸（勋章 min 92 / 印章 min 64）。
- **iOS 有、网页端不做的一条**：`EarnedToast` 在首页回归卡占位时让位。网页端**没有回归卡**（那是 iOS `HomeView` / 安卓 `HomeScreen` 的东西），不存在抢位问题。
- **踩到的坑**：勋章图用 `onLoad` 控制淡入时，**图若已在浏览器缓存里，onLoad 在 React 挂上监听之前就过去了**，图会永远停在 `opacity: 0`。必须在 ref 回调和 `useEffect` 里各查一次 `img.complete && naturalWidth > 0`。
- **日期**：2026-09-19

## 2026-09-19 · 划重点四色板改版 + 跟读高亮按句

**决定了什么**

1. **划重点色板换掉三支笔**：`#7BC96F`（GitHub 绿）/ `#0FBCDB`（纯青）/ `#F48FB1`（Material 粉）
   → `#A3B565` 橄榄绿 / `#4E86A0` 青石蓝 / `#C0625F` 石榴红。灯油黄 `#FFB103` 保留（品牌色）。
2. **高亮一律「颜料 + alpha」铺在羊皮底上**，不再用不透明色块。alpha 表：
   黄 .45/.28、绿 .50/.28、蓝 .46/.32、红 .42/.30（浅色/深色）。
   四端各有一个 `verseTextHighlightFill` / `fillColor` / `verseHighlightFill` 入口，
   **渲染端不许直接拿调色板 hex 当背景色**。
3. **带旧→新 hex 迁移表**，写在各端 `normalizeColor` 里。
4. **色板按钮显示合成色**（所见即所得），不是纯颜料色。
5. **跟读高亮从「整节铺底」改成「按句、贴着字铺」**。时间轴只精确到节，
   节内按非空白字数比例插值定位当前句（`VerseSentences`）。搜索定位仍保留整行框。
6. 跟读黄 alpha .55/.28→.34，比划重点的 .45 重一点，好和「我自己划的」分开。
7. `humanSpeech` 深色值：网页 / RN / 安卓原来照抄浅色的 `#38486C`，压在 `#1a1512` 上看不见，
   统一到 iOS 早就在用的 `#9DB2DE`。**浅色的 `#38486C` 不动**——它同时是旧约强调色
   （`canon-section-theme.ts`），是品牌的一部分。

**为什么**

- 用户反馈「加亮色彩整体不匹配风格」。主色系（羊皮卷 / 沉香木，hue 25–45 暖色家族）是自洽的，
  违和的只有加亮层那三个从别处抄来的 stock 颜色。完整推导见 `docs/design-color-system.md`。
- 「按行高亮」是因为跟读只定位到节，长节铺满三四行，和耳朵听到的位置对不上（Josh 2026-09-19）。
  按句插值后误差被限制在一节之内，每到节边界自动重新对齐。

**范围**：色板四端全改（数据要互通）。跟读高亮原先只有 iOS / 安卓原生是活的，
网页 `activeIndex` 和 RN `activeVerseIndex` 都写死 null；Josh 2026-09-19 要求**重新打开**，
四端现在都是「按句」。

## 2026-09-19 · 网页 / RN 跟读高亮重开

**决定了什么**：把网页 `ReadChapterVersesClient.activeIndex` 和 RN `useReadChapterAudio`
里写死的 null 去掉，跟读高亮四端都打开，并且一上来就是「按句」。

**为什么当初关掉、现在怎么绕开**：

- 原因一「易错位」→ 认不出的译本根本拿不到时间轴（网页 `buildChapterVerseTimingsCandidates`
  返回空、RN `loadBundledChapterVerseTimings` 返回 null），**没有时间点就不高亮**
  （Josh 2026-09-11 定的规矩），不会再去套别的译本的时间轴。
- 原因二「跟读态抬重渲染」→ 派生值先收敛成一个字符串 `节号:句下标`：
  RN 走 `useScriptureFollowDerived`（快照不变直接跳过重渲染），
  网页用 ref 缓存住 key 不变时的同一个对象。句几秒才换一次，重渲染频率比原来「按节」还低。
  句切分表在 `useMemo` 里切一次缓存住，派生函数只做二分 + 累加。
- **自动滚到屏幕中间仍然不做** —— 那是另一个问题（滚屏抢手势），这次没碰。

## 2026-09-19 · 全 App 只留一种绿

**决定了什么**：`#34C759`（iOS 系统绿）全部下掉，收敛到仓库里早就有的
`READ_DONE_ACCENT = "#65775C"`（橄榄绿），并补一个深色版 `READ_DONE_ACCENT_DARK = "#A8BC72"`。
落点：人生电池「还剩」（网页 + RN）、金句坞开关。

**为什么**：不新造色 —— `#65775C` 本来就在读经目录「已读完」和今日统计里用着，
本来就在羊皮家族里。「读完 / 已获得 / 还剩多少」这类正向状态以后一律用它。

## 2026-09-19 · 安卓真机侧载用 sideload 变体

**决定了什么**：新增 `sideload` buildType（= release + `.native` 包名后缀），
真机验证一律装它；`assembleRelease` 只留给 Play / 下载页。

**为什么**：手机上的 `me.askbible` 是 Play App Signing 重签的，本地包覆盖不上；
而卸载重装会清掉登录和设置。「装到手机一律 Release」（APP/CLAUDE.md 7.0）
和「别清真机数据」这两条规矩，靠这个变体同时满足。对应 8.2 里的
`gradle_task: assembleSideloadRelease` 那个已有模式。

## 圣经故事解读做成技能，成品先放私域

- **决定了什么**：整套流程固化为技能 `.claude/skills/bible-story/SKILL.md`（简写 bible-story）。成品**先放私域**——Josh 自己听、自己测试，**不进 App、不上 R2**。稿子入库（`docs/story-scripts/`），音频不入库（`private/bible-stories/audio/`，已加 .gitignore）。
- **为什么**：内容口径和听感还在摸索期，先自用验证，确认稳定了再谈上线形态。这也顺带把「成品放哪」那个悬了三轮的问题解掉了——暂时哪都不放。
- **技能里固化了什么**：取经文的 sqlite 命令与两个文本坑、写稿的八条要点（正统释经路线，非鸡汤）、中文 250 字/分钟的长度换算、NotebookLM 完整操作步骤与 A/B 两版指令模板、以及**五条已验证走不通的下载办法**（挂 window.open 钩子会把真实下载一起拦死、CORS、navigate 被禁、curl 拿到登录页、Chrome 是另一个 Google 账号）。
- **下载这一步保持人工**：浏览器面板只允许用户自己的点击开新标签，Claude 点必被拦。流程是把 ⋮ 菜单摆好、请 Josh 点一下。一集一到两下，成本最低，不再找绕路。
- **日期**：2026-09-19

## 2026-09-19 · 会员同步 flushNow 不能写成递归（真机启动几秒必被杀的根因）

- **决定了什么**：`MemberReadingSyncEngine.flushNow` 在 iOS / 安卓两端一律改成
  **循环 + 等完就地清 `inFlight`**，不再递归自调。
- **症状**：真机上 App 启动几秒就消失，**没有任何崩溃报告**。手机的
  `JetsamEvent-2026-09-19-145648.ips` 里记着 AskBible 连被杀三次，
  `reason = per-process-limit`，每次 `rpages = 134272`（= 2098 MB，撞穿 iPhone 12 的
  单进程上限），`cpuTime` 只有 6.3～6.8 秒。
- **根因**：`flushNow` 里「已有同步在跑就等它完再跑一轮」写成了递归：
  ```swift
  if let t = inFlight { pendingFlushReason = reason; let o = await t.value
      guard let again = pendingFlushReason else { return o }
      pendingFlushReason = nil; return await flushNow(reason: again) }   // ← 递归
  ```
  `inFlight` 的清理挂在 `schedule()` 里**另一条** Task/协程上，所以 `await t.value`
  返回时 `inFlight` 往往还指着那个**已经跑完**的 task。递归回来又 await 一个完成态
  task —— 立即返回、不让出任何时间，`pendingFlushReason` 还是自己刚写进去的，
  于是无限递归。它把 MainActor 占死，负责清 `inFlight` 的那条 Task 永远排不上队，
  循环再也出不来。
- **为什么只有登录用户中招**：入口第一行就是 `guard loggedIn`。空模拟器不复现，
  把真机的 `Library/Preferences/me.askbible.plist`（含 sessionToken）灌进模拟器后必现。
- **实测数据**：修复前峰值 **16313 MB**（4 秒破 2 GB，真机就是在这儿被杀）；
  修复后同样条件峰值 **323 MB**，40 秒平稳。`heap` 显示 820 万个 "Task stack" 占 8.4 GB，
  `malloc_history -callTree -invert` 把 5.27 GB 直接指到 `MemberReadingSyncEngine.swift:328`。
- **网页 / RN 端不用改**：它们靠 Promise `.finally()` 的时序 —— 清 `syncInFlight = null`
  发生在 `await` 返回**之前**，不存在这个窗口。这是三端对等写里少见的「写法不同反而救了一端」。
- **留下的规矩**：`inFlight` 这类「谁在跑」的句柄，**清理必须和等待在同一条执行流里**。
  只要清理另起 Task，等待方就必须在 await 之后自己再判一次、就地清掉，绝不能凭
  「它应该已经被清了」继续往下走。
- **日期**：2026-09-19

## 2026-09-20 · 成就反馈 v2：声音分层，章完成升为核心事件

**背景**：v1（前一天）三个事件都有声音、都偏「奖励音」。这轮和 ChatGPT 对过一轮，
它的判断和我自己的怀疑一致：**「钟」的存在感略重，三个事件都靠钟来定义是错的**。

**定下来的反馈哲学**（这条是纲，后面所有取舍都从它推）：

> **小事靠触感，完成靠声音，成就靠钟声，升级靠光。**

**决定了什么**

1. **音色分三层**：木叩（小事）→ 小钵（完成）→ 钟（成就 / 升级）。越重要余韵越长。
   - `xp` 木叩：噪声激励 + 520/880/1450Hz 共振，70ms。原来是 1760+2637Hz 正弦，
     太电子 —— 高频正弦连响几十次非常累耳。
   - `chapter` 小钵（**新增**）：330Hz，0.55s。比木叩有余韵、比勋章钟收得快。
   - `earn` 单钟 440Hz 1.1s / `levelup` 三声上行钟 440–554–659Hz 1.9s，
     间隔 130ms（GPT 建议 100–140ms），最后一声 release 最长。
   - 钟和钵都卷了一个很短的合成混响（指数衰减噪声 IR，wet 14–22%）。
     **干声合成的钟「太近」，听着像合成器 —— 这是廉价合成音最大的破绽。**
2. **+XP 小额不出声，只给触感**；只有整章完成才出声，而且**用小钵不用木叩**
   （木叩配不上「读完一章」这件事）。
   理由：几十秒响一次，长期一定变成「游戏化噪声」；把声音留给有意义的完成节点，
   声音本身才珍贵。
3. **木叩音量 0.45 → 0.34**（GPT 建议 0.30–0.35）。
4. **升级不加全屏粒子 / confetti / 闪白**。保留「金边呼吸 + 金光扫过 + 三声钟」，
   把高级感做在时间、衰减、克制上，而不是加元素。粒子会直接把视觉语义拉向游戏产品。
   - 金光扫：1.1s → **0.62s**，峰值白 0.55 → **0.22**（GPT：500–700ms / 15–25%）
   - 金边呼吸：0.75s → **1.35s**（GPT：1.2–1.5s）
5. **网页尊重 `prefers-reduced-motion`**：开了「减弱动态效果」就不跑弹入 / 光扫 / 呼吸。
   声音不受这个偏好影响 —— 那是两种不同的偏好，别混。
6. **高亮 / 收藏保持无声**（现状就是只有触感），确认不加。高频操作出声会污染阅读环境。

**为什么记这条**：下一个来加游戏化功能的人，最容易犯的错是「给每个系统事件都配一个奖励音」。
上面那句纲领就是用来挡这件事的。

## 2026-09-20 · 状态色锁成四档；开关 on 态是 accent 不是成功绿

**决定了什么**：`docs/design-color-system.md` 第四节从「建议表」升级成**硬规则**——
全 App 状态色只有成功 / 提醒 / 危险 / 禁用四档，**要加第五个先改文档再改代码**，
不许在组件里就地写新 hex。另显式写死两条：

1. **开关 on 态、选中态一律用 accent（LOGO 黄 `#FFB101`）**，不是成功绿。
   iOS `Switch` 默认系统绿、安卓 Material `Switch` 默认主题色，**两个平台都必须显式覆盖**。
2. **中性徽章 / 标签不给新颜色**，用 `muted` 文字 + `border` 描边。

**为什么**：9-19 收敛掉三种绿时，我把金句坞开关的 on 态也收进了成功绿 `#65775C`——
**映射错了**。「这项设置开着」和「读完 / 已获得」是两套语义；而且后来成就墙那三个开关
用的是 accent 金，同一个 App 两个开关两种颜色，正是要防的那个病。已改回 accent。

收敛主色之后真正会复发的不是主色，**是后面新功能随手加的第四第五个语义色**
（`#7BC96F` / `#0FBCDB` / `#F48FB1` / `#34C759` 当初就是这么一个一个进来的），
所以规则先落盘。四档抽成三端共享 token 仍在 `docs/OPEN-ITEMS.md` 等 Josh 定。

## 2026-09-21 · 读经页播放坞降一档：安静档（quiet）

- **决定了什么**：`PlaybackDock` 新增 `quiet` 档，只给**读经页**用（计划播放页不开）。
  安静档去掉封面缩略图和章名两个元素，控件框 48×44 → 40×40、播放键 42 → 36、
  「下一章」只留图标不带两行文字、外层竖向内边距 8 → 6、两行间距 8 → 4。
  整块从约 108pt 降到约 70pt。触控框仍然 ≥ 40，没有回到「图标小到会误点」那一版。
- **为什么**：Josh 2026-09-21「播放栏太大了，也太吵，影响阅读」。读经页的主角是经文，
  坞只要够按；封面和章名在读经页是冗余信息——章名页顶已经有了，封面是借的自然场景图。
  计划播放页不同，那页坞本身就是主角，所以按 `quiet` 参数分档，不是全局改小。
- **没动安卓**：安卓 `PlaybackDock.kt` 走的是**非 compact 的单栏布局**，本来就没有封面和章名，
  高度约 80dp，不存在同一个问题。要动等 Josh 在安卓上也提。

## 2026-09-21 · 安静档第二刀：降对比，不砍功能

Josh 说「你决定」，三件事一起定了：

- **播放坞再压一档**（只在读经页的 `quiet` 档生效）：没加载出时长前整行进度**不显示**
  （`00:00 / —:—` 是纯噪声）；语速 / 循环 / 搜索三个次要图标对比度 0.75~0.8 → 0.55~0.6；
  琥珀播放键**在没播时**压到 0.82 透明度，播起来才满金；底玻璃 0.78 → 0.62。
- **放弃了的路**：① 把语速 / 循环 / 搜索收进「…」——省 20pt 却让每次切语速多一次点击，
  读经时最常调的就是语速，不划算；② 不播放时整条坞收成一根细线——发现性太差，
  新用户会找不到朗读入口。两条都不做，**降对比不砍功能**是这一刀的原则。
- **外部 Beta 审核不撤回**：build 130 就按正常节奏发给外部测试员。
- **版本号留 1.1.2**：1.1.1 已上架、129 已占用，1.2 留给下一个有分量的功能版。

## 2026-09-21 · 读经页播放坞不再用玻璃，改不透明深木底

- **决定了什么**：读经页的坞（`quiet` 档）背景从半透明玻璃改成**完全不透明**的深沉香木
  `#241A13`，前景全部换成纸色 `#ECD9B9`（时间 0.72 / 语速 0.66 / 循环 0.66~0.95 /
  下一章 0.78 / 搜索 0.66），进度轨改纸色 0.26，琥珀播放键保持满金（深底上不刺眼）。
  圆角从 `AskCorner.control` 改成 `AskCorner.sheet`——外层 `askGlassRect` 就是这个半径，
  用小半径会在四角露出一圈玻璃，经文照样透上来。
- **为什么**：Josh 2026-09-21「透明玻璃在影响阅读，还不如原来的黑播放栏」。
  坞浮在经文上（`.overlay` 不是 `safeAreaInset`，见 ShellTabBar 注释），玻璃底下永远有经文在滚，
  字压字是最难读的一种。**阅读面上的浮层要挡住，不要折射**——玻璃留给不压正文的地方。
- **作废**：同日早些时候那条「底玻璃 0.78 → 0.62」和「未播时琥珀键压到 0.82」，
  前者被本条取代（根本问题是透明本身），后者在深底上没必要、压了反而发脏。
