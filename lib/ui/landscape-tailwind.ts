/**
 * Tailwind 任意媒体：手机/小平板横屏（与 `useLandscapeNarrow` 条件一致）。
 * 用法：\`${landscapeNarrowMedia}:flex-row\`
 */
export const landscapeNarrowMedia = "[@media(orientation:landscape)_and_(max-width:1024px)]";
