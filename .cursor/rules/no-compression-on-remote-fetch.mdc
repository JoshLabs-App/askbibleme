---
description: Do not compress remotely fetched media files
alwaysApply: true
---

# 外站拉取文件禁止压缩（强制）

当任务涉及从其它网站拉取/同步文件（尤其是圣经音频）时，默认与强制要求如下：

- 禁止在拉取链路中做任何压缩、降码率、转码、重编码。
- 禁止使用 `ffmpeg`、`sox` 或其它工具对拉取到的源文件做质量变更。
- 必须原样保存来源文件（byte-preserving copy/download）。
- 如现有脚本包含压缩步骤，修改为“直下直存”，不要新增“可选压缩”默认项。

若用户明确提出“压缩以节省空间”，也先按本规则执行并提醒：该仓库约定外站拉取文件不压缩；需用户再次明确覆盖此规则后才可改动。
