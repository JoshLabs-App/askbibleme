---
description: 默认按 App 优化优先推进需求
alwaysApply: true
---

# 默认 App 优化优先（开发计划）

当需求未特别说明平台时，默认按「App 优化优先」执行：

- 先以 `apps/askbible-mobile` 的交互、视觉、性能为实现与验收基线。
- 设计与实现默认面向移动端真实使用场景（单手操作、可读性、低干扰、低认知负担）。
- 若同一能力涉及多端，先完成 App 版本，再同步其它端。
- 未被明确要求前，不主动增加仅 Web 端收益但会抬高复杂度的实现。

## 评审与回归检查

- 优先检查 App 端是否达到可用与流畅，再检查其它端一致性。
- 出现平台差异时，先说明限制，再给出最接近 App 体验的降级方案。
