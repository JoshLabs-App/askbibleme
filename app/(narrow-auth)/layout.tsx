import type { ReactNode } from "react";
import "../(app-shell)/narrow-parchment.css";

/** 登录 / 注册 / 找回密码：默认全站羊皮卷底 + 窄栏版心 */
export default function NarrowAuthLayout({ children }: { children: ReactNode }) {
  return children;
}
