<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- 与 pbxproj 里自动生成的键合并（GENERATE_INFOPLIST_FILE = YES）。
	     UIBackgroundModes 不是 Xcode 认的 INFOPLIST_KEY_*，写在构建设置里会被无声丢掉（2026-09-09 真 iPhone 打包出来的 plist 里没有它），所以放这里。 -->
	<key>UIBackgroundModes</key>
	<array>
		<string>audio</string>
	</array>
</dict>
</plist>
