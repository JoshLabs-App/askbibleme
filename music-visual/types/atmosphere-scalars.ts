/** 与 `MusicVisualAtmospherePreset` / shader uniform 对齐的标量（供 CSS 与 WebGL 共用） */
export type MusicVisualAtmosphereScalars = {
  fogSpeedMul: number;
  glowWeightMul: number;
  particleDensityMul: number;
};
