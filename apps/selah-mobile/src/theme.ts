/** 与前台品牌 `canvas` / 底栏 `#143C60` 对齐（与 `lib/site-branding-colors` 默认同系） */
export const theme = {
  canvas: "#143C60",
  surface: "#183A52",
  ink: "#F1F5F9",
  muted: "#9BB0C2",
  border: "rgba(41, 88, 120, 0.55)",
  sand: "#7EB8E0",
} as const;
