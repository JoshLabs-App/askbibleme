---
description: Mobile media playback uses local files first; remote only for package updates
alwaysApply: true
---

# 移动端媒体播放与更新包（强制）

适用范围：`apps/askbible-mobile` 的音乐/视频/音频相关实现与排障。

- 播放链路默认且强制：**只使用设备本地可用媒体**（安装包内、已下载到本地资源包、DocumentDirectory 缓存）。
- 远端（如 `askbible.me`）默认只用于：**检查更新包、下载更新包、拉取 manifest 元数据**。
- 未进入“更新包流程”前，禁止把远端媒体 URL 直接接入播放器作为常规播放源。
- 排障顺序必须先查本地链路：本地资源是否存在、索引是否命中、路径是否可读、格式是否可播。
- 只有用户明确要求“临时在线流式播放”时，才可增加远端直连播放，并在改动说明中写明是临时策略。

## 实施提示

- 先修“本地媒体缺失/索引错误/打包未同步”，不要先改成网络兜底播放。
- 更新包下载成功后再切换到新本地资源；切换前继续使用旧本地资源，避免出现“可见曲目但不可播”。
