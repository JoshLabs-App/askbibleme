# 01AskBible · 待办 / 待 Josh 决定

每条写清三件事：现状 / 影响 / 需要 Josh 决定什么（给推荐选项）。
做完的移到「已关闭」，注明日期，不要直接删。

## 待决

### RN 版目录的「›」要不要一起去掉
- **现状**：原生 iOS / Android 已去掉行尾箭头，RN 版 `apps/askbible-mobile/src/read/BibleCatalogBookRow.tsx` 的 `bookChevron` 还在。
- **影响**：三端目录外观不一致；RN 版正在被原生替代，改与不改都不阻塞。
- **需要 Josh 决定**：推荐不改，等原生完全接管后 RN 版一起退役；若你还在用 RN 包发版，说一声我同步删掉。

### 底栏以外的图标按钮可能还有同样的点不中问题
- **现状**：只修了 `ShellTabBar`。目录右侧竖排和其它页的 MaterialIcon 按钮有的已补 contentShape，有的没排查过。
- **影响**：个别按钮要精确点在笔画上才响应，真机上很容易被当成「没反应」。
- **需要 Josh 决定**：推荐我做一轮全量排查，给所有图标按钮统一补上；要不要现在做由你定。

### 人物立绘出图时输入框是共用的，会互相覆盖
- **现状**：我用 `form_input` 写 ChatGPT 输入框，Josh 也在同一个会话里手打纠正。我写值会把他正在打的字整段覆盖掉。
- **影响**：两边同时动会丢消息、发错内容；目前我已停手等他。
- **需要 Josh 决定**：推荐规则定完后由我独占操作，你有意见就在这边跟我说、我代发；或者你继续手动，我只负责最后批量取图。

### 扫罗不在这一批 12 个核心人物名单里
- **现状**：Josh 手动生成了扫罗，人物库里 `saul` 属于 supporting，不在 `importanceTier == "core"` 的 12 人里。
- **影响**：图已经有了，留着不浪费；但会让「第一批 = 12 核心」的口径不准。
- **需要 Josh 决定**：推荐照收，当作第一批的附赠，视觉库里照样建档。

### 人物立绘的假透明和写实漂移还没验完
- **现状**：后半批图是 RGB 无 alpha，棋盘格是画上去的；风格也从动画漂成真人写实。已在 ChatGPT 会话里发了纠正并重做摩西，结果没验就停了。
- **影响**：这批图现在不能直接进项目，拿去抠图也会连棋盘格一起抠。
- **需要 Josh 决定**：推荐下个线程先验摩西那一张，通过了再批量重跑；不通过就退回前半批那种写法。

### 人物立绘管理网页还没做
- **现状**：Josh 要一个页面，列出已生成和待生成的人物，点一下能去生成或重新生成，并用新图替换旧图。出图还没定版，页面没开工。
- **影响**：现在挑图只能靠翻 ChatGPT 会话，重跑哪几个全靠记。
- **需要 Josh 决定**：推荐等出图定版后再做，做成本地 admin 页；点「生成」写进一个待跑队列文件，由我读队列去浏览器跑。

### info-edition.sqlite 首次下载无法离线使用

- **现状**：文件已改为按需下载，首次打开「陪你探索 / 查找资料」需要联网下载 26MB；下载后永久存 filesDir / Documents，离线可读。
- **影响**：完全没网络时点进去会显示「首次加载中」一直转，不会报错（HTTP 连不上会走 Failed 状态，有"重试"按钮）。
- **需要 Josh 决定**：是否需要在 App 设置页里加手动预下载按钮（像 KJV 那样），或保持现在「用到再下」的行为即可。推荐保持现状。

## 已关闭

### 讲道配音用真人还是 AI 声音（已关闭 2026-09-14）
- Josh 定：信里不写配音方式。

### 讲道配音的范围（已关闭 2026-09-14）
- Josh 定：信里不写具体范围。

### 给 Desiring God 的第二封信（已关闭 2026-09-14）
- 按定稿在原邮件串回复发出，等 DG 回复。


## 划重点清除后被补回：iOS 原生 / 网页 / RN 仍是并集合并（2026-09-14）
- **现状**：安卓原生已改为整份后改为准（见 DECISIONS）；iOS `MemberReadingSync.swift`、网页/RN `mergeReadingBlobs.ts` 仍逐字并集。`check:member-sync` 对拍会在 highlights 上与 RN 不一致。
- **影响**：在 iPhone 或网页上擦除仍会复活；另一端用并集推送时，也可能把安卓擦掉的内容再合回云端。
- **要 Josh 决定**：是否三端统一改成同样规则（推荐：统一改）。

## 已关闭（原生安卓发布）
- 2026-09-14：1.0.43(240) 真机验证通过（RN 版覆盖更新后收藏/计划保留），已推 Play production 轨道。注意：RN 登录态不迁需重登；设置回默认；官网 APK 与 Play 包签名不同。
- 2026-09-14：RN→原生本地数据迁移——已做，见上条。

## 首页图标分散（2026-09-16）— 已关闭 2026-09-16：Josh 选 A，已实现
- **现状**：左上菜单、右上齿轮（小圆玻璃钮），中下金句，下面是三个专辑开关，再往下是底栏，图标散在四处。
- **影响**：全景自然图被切得很碎，不像沉浸式的背景。
- **待决定**：推荐 A：去掉右上齿轮，设置并进底部一块玻璃；闲置后只留金句和底栏。备选 B：只把齿轮和菜单并到左上；C：保持不变，只统一大小和样式。

## 首次打开的第 3 屏「今日读经」引导还没做（2026-09-18）— 已关闭 2026-09-18：定为不做，见 DECISIONS
- **现状**：欢迎页已重做，主按钮落到首页（首页即灵修）。但三个主场景里的第二个——每日读经——在首次体验里仍然没有引导，用户要自己去点底栏的「读经」。
- **影响**：场景排序只落实了第一段。
- **要 Josh 决定**：要不要给「读经」加一个首次引导（比如首页闲置一段时间后，或第二天打开时提示今日读经）。注意这条和「首页不被打扰」直接冲突，**我的推荐是不加**，让读经维持在底栏入口。

## 「今日灵修」的内容池只有 3 篇（2026-09-18）— 已作废 2026-09-18：灵修不是文章，见 DECISIONS
- **现状**：灵修正文取自探索文章，去掉读经计划器那篇占位后只剩 3 篇，所以三天一轮回。
- **影响**：用户第四天会看到和第一天一样的「今日灵修」，"每日"名不副实。
- **要 Josh 决定**：是补文章（每天一篇要 30+ 篇起步），还是改成每周一篇、首页标签写「本周灵修」。推荐先改成「本周灵修」，等内容跟上再回到每日。

## 金句和当天那篇灵修文章内容不相关（2026-09-18）— 已作废 2026-09-18：首页灵修卡已撤，见 DECISIONS
- **现状**：金句来自 home-verse 池并轮播，灵修文章独立选取，两者没有关联；ChatGPT 的理想结构是「一篇灵修 = 一个主题 + 一处核心经文 + 一篇短文」。
- **影响**：首页上下两块内容各说各的，"金句是这篇灵修的引子"这个规则目前只在文档里成立，界面上并不成立。
- **要 Josh 决定**：要不要给每篇灵修文章配一节固定的核心经文（文章数据里加一个字段），首页在这块显示那一节而不是轮播金句。推荐配，但等上一条的内容量决定之后再做。

## 「下午茶」这个名字在今日灵修语境下有点跳（2026-09-18）— 已关闭 2026-09-18：不改名，下午茶与专注是两个东西，见 DECISIONS
- **现状**：首页工具排里「下午茶」实际是专注计时。ChatGPT 建议改叫「专注」。
- **影响**：只是命名，不影响功能。
- **要 Josh 决定**：这是你起的名字，带品牌意味，我没有擅自改。要不要改叫「专注」。

## 登录 sheet 的主按钮底色还是深色叠加（2026-09-18）— 已关闭 2026-09-18：已统一成琥珀色
- **现状**：`AuthSubmit` 用 `0x1C1410/0.1` 的深色叠加做底，和这轮统一的 `0xF8F1E3/0.72` 纸色控件不是一套；登录页 / 注册页也共用它。
- **影响**：sheet 里三个按钮（Apple / Google / 邮箱登录）是纸色，往下的「登录」主按钮是灰调，材质不统一。
- **要 Josh 决定**：主 CTA 要不要统一用欢迎页那颗琥珀色（`0xffb101/0.22`）。推荐：统一用琥珀，主次一眼分得开。

## 留存与反馈机制：要不要「像游戏一样」（2026-09-18）— 已作废 2026-09-18
- 本条原提案（`docs/12-retention-feedback-proposal.md`，已删）主张拒绝游戏化外壳，撰写时没看到同日已落盘的成就 / XP 决策，与之直接冲突。
- **有效结论以 `docs/DECISIONS.md` 三条为准**：「成就系统：复用 MY CLASS 勋章图」「XP 要一直在涨」「XP 刺激强度定 A 方案」。
- 其中唯一未被覆盖、仍成立的缺口另立一条，见下方「离开之后为什么回来」。

## 回归卡还没接线（2026-09-18）— 已关闭 2026-09-18：两端已接线并实测通过
- **现状**：决策已定（见 DECISIONS「留存的缺口是『回归』」），四条文案键已加并生成到两端 SiteCopy。**界面接线没做**——本轮有另外两个会话正在改同一批入口文件：`01askbible-21` 在改 `AskBibleApp.swift` / `MainActivity.kt` 的 `syncEngine.attach`，会话「苹果虚拟机热版本安装」在重做 `HomeView.swift`（首页闲置隐藏底栏）和 `WelcomeView.swift`。按 `APP/CLAUDE.md` 6.4，撞车就先停手，所以没动。
- **影响**：文案键目前是死的，没有任何界面读它。不接线就没有回归效果。
- **已做（2026-09-18）**：两个会话都已收工放行，接线完成。iOS `Home/HomeView.swift` + `AskBibleApp.swift` 调用处、安卓 `ui/HomeScreen.kt` + `MainActivity.kt` 调用处。显示条件＝最近阅读的那一天不是今天；跟着首页闲置一起淡出（7 秒），点过就不再出。点一下跳那一章，无记录时只进读经页。两端模拟器实测：卡片出现 → 点击 → 直达马太福音 13。

## 回归卡和 XP 升级横幅抢同一个顶部位置（2026-09-18）— 已关闭 2026-09-18：Josh 定「你决定」，按推荐做了让位
- **现状**：iOS 实测时，冷启动头几秒顶部先弹「升到 4 级」的 XP 横幅，位置和回归卡完全重叠，横幅退场后回归卡才露出来。安卓没撞上（横幅样式不同），但结构上是同一个槽位。
- **影响**：刚回来的用户第一眼看到的是等级横幅而不是「我们上次停在这里」，回归卡的首屏作用被削弱。不是 bug，两块都能正常工作。
- **已做（2026-09-18）**：回归卡在 / 不在通过 `onReturnCardVisible` 报给壳（iOS `homeReturnCardUp`、安卓同名），横幅 `EarnedToast` 在卡片在时不渲染。事件**不消费**，留在 `ach.pending` 里，卡片退场后照常补弹。iOS 模拟器实测：有回归卡的那次冷启动横幅不再弹（此前每次都弹）。补弹那一半没能在模拟器上另造一次升级事件复现，是按「EarnedToast 只在自己显示时的 .task 里消费事件」推出来的。安卓同样改了，但安卓冷启动本来就没撞上这个位置冲突，属防御性对齐，只过了编译。

## 全新用户第一次打开就会看到「从这里开始」卡（2026-09-18）— 已关闭 2026-09-18：Josh 定「现在没什么用户，可以加」，保留不改
- **现状**：按 DECISIONS「首次或无记录时退化成『从这里开始』」实现，所以没有任何阅读记录时卡片照出。
- **影响**：和「首页的价值是全景不被打扰」有张力——同日另一个会话在首页加的「今日灵修卡」就是因为这条被你一句话推翻了。我已经让它跟着闲置淡出、点过不再出，但首屏那几秒确实压在风景上。
- **Josh 定（2026-09-18）**：「不过现在没什么用户，所以可以加的」——保留「从这里开始」这一档，代码不改。等有真实用户量之后如果发现它干扰首屏，再回来砍。

## `npm run check:member-sync` 跑不起来（2026-09-18）— 已关闭 2026-09-18：已修，164 条用例三端通过
- **现状**：对拍脚本用裸 `swiftc`（macOS 目标）把 iOS 侧几个文件编成命令行 harness，其中 `Theme/ParchmentTheme.swift` 在深色模式那次提交（`e6bab918`）之后用了 `UIColor { traits in ... }`，macOS 目标下报 `cannot find 'UIColor' in scope`，整个 check 直接崩。`BibleCatalog.swift` 依赖 `Color(parchment:)`，所以不能简单把 ParchmentTheme 从文件列表里去掉。改用 iOS 模拟器 SDK 交叉编译也不行——产出的二进制在 macOS 上跑不了。
- **影响**：三端（Swift / Kotlin / TS）会员同步规则的一致性检查**目前完全没在跑**，`npm run check:native` 也会在这一步挂掉。本轮新加的 `achievements` blob 三端合并规则因此只有人工对照，没有机器对拍兜底——这正是这个 check 存在的理由。
- **要 Josh 决定**：怎么修。推荐 **A：给 `ParchmentTheme.swift` 里那个 `init(parchment:)` 包一层 `#if canImport(UIKit)`**，非 UIKit 平台退化成不带深色映射的纯色（harness 只需要它能编过，不看颜色）——改动 5 行、不影响 App 行为。B：把颜色从 `BibleCatalog.swift` 里剥出去，让 harness 不再牵扯主题——更干净但改动面大。修好之后还应在 `tools/member-sync-expect.mts` 的 CASES 里补几条 `achievements` 用例。

- **怎么修的**：`Theme/ParchmentTheme.swift` 的 `init(parchment:)` 包了一层 `#if canImport(UIKit)`，非 UIKit 平台（对拍 harness 跑在 macOS 目标上）退化成不带深色映射的纯色。App 行为不变，iOS 重新编译通过。
- 顺手把 `achievements` 的 5 条用例补进 `tools/member-sync-check.mjs` 与 `tools/member-sync-expect.mts`（expect 里是独立重写的一份，不 import 真源，才起得到对拍作用），三端结果一致。

## iOS 擦掉的划重点会被云端旧副本补回来（2026-09-18）
- **现状**：对拍恢复之后暴露出来的既有不一致。安卓的 `mergeBlobValue("highlights")` **整份以时间戳新的一侧为准**（`core/.../MemberReadingSync.kt` 有注释说明：并集会让擦掉的重点被旧副本补回），并且 `VerseHighlightStore` 有 `localUpdatedAtMs` 配套；iOS 与 TS 仍是**逐章并集**，没有这套时间戳。对拍脚本里已把这一条列进 `KNOWN_DIVERGENCE` 白名单显式放行（跑起来会打印一行 `!` 提示），所以 check 是绿的，但**不一致是真的**。
- **影响**：iOS 上擦掉一条划重点，如果另一台设备或云端还留着旧副本，下次同步会被补回来，用户看到「擦不掉」。安卓没这个问题。
- **要 Josh 决定**：要不要把 iOS / TS 对齐到安卓的语义。推荐**对齐**——擦除是明确的用户意图，不该被旧副本推翻；代价是 iOS `VerseHighlightStore` 要补 `localUpdatedAtMs` 那套时间戳（安卓已有，照搬即可），属于独立一件事，建议单开线程做。不做也不会更糟，是既有行为。

## 网页端成就系统（2026-09-19）

Josh 2026-09-19「你决定」——下面四条按我的推荐处理完了，保留在这里是为了记住**为什么**这么定，以及剩下的那一条尾巴。

## 安卓侧载变体 sideload（2026-09-19 新增）

**现状**：手机上的 `me.askbible` 是 Play 装的（`installerPackageName=com.android.vending`），
被 Play App Signing 重签过，本地 upload key 打的 release 包覆盖不上去
（`INSTALL_FAILED_UPDATE_INCOMPATIBLE`）。卸载重装会清掉 Josh 手机上的登录和设置。

**做法**：`apps/askbible-android/app/build.gradle.kts` 加了 `sideload` buildType，
`initWith(release)` + `applicationIdSuffix = ".native"`。同一把 upload key、不开 minify，
唯一区别是包名变 `me.askbible.native`，和商店版并排装。

```bash
cd apps/askbible-android && ./gradlew :app:assembleSideload
adb -s <三星> install -r app/build/outputs/apk/sideload/app-sideload.apk
```

**以后装真机一律用这个**，不要再打 `assembleRelease` 往手机上怼。
`.android-deploy.json` 不动 —— 那条路是发 Play / 下载页用的，必须是 `me.askbible` 正包。

## 成就反馈：还没做的（2026-09-20）

反馈哲学与已落地部分见 `docs/DECISIONS.md` 同日那条。下面是 ChatGPT 建议里
**我认可但这轮没做**的，需要 Josh 决定要不要推进：

1. **「读完一章」的动效还没做**（推荐：做）。
   现状：章完成现在有**声音（小钵）+ 触感**了，但**没有专属动效** ——
   还是复用 +XP 的飘字。GPT 的建议是「阅读进度线完成 → 金色短暂亮起 → 回归静止，
   **不要弹 Toast**」。
   影响：这是读经 App 真正的核心完成时刻，现在的反馈只做了三分之二。
   要决定：动效挂在哪儿 —— 章页底部的进度条？还是顶部的章节标题？
   这要改读经页的布局，不是纯加动画，所以我没擅自动。

2. **连续天数（streak）完全没有反馈**（推荐：做，价值最高）。
   现状：`streakDays()` 只参与算倍率，**延续 / 中断都不发事件**，界面上毫无动静。
   影响：连续天数是日活 App 最强的留存杠杆，现在它是「隐形」的。
   要决定：延续时给什么（我的建议：当天第一次完成时，轻触感 + 小钵，不弹卡片）；
   **中断时给什么 —— 我的建议是什么都不给**，安静地把数字归零就行，不要用声音惩罚用户。
   这条要动 `AchievementStore` 发新事件，三端都要改。

3. **今日计划完成没有反馈**（推荐：跟第 2 条一起做）。
   GPT：「加入计划」只给触感 + 状态变化；「完成今日计划」才给声音。现在两者都没有。

4. **语义色四档抽成三端共享 token**（老条目，规则已锁，只剩结构性改动）。

### 已关闭

- **听读 XP 没有数据源 → 已做**：`lib/read/scripture-listen-totals-web.ts` 新增 `addScriptureListenSecondsWeb()`（并补上 `writeScriptureListenTotalsWeb` 一直漏掉的变更通知——此前订阅者根本收不到通知）；`components/music/MusicShellPlaybackContext.tsx` 里按**媒体时间增量**每满 15 秒记一片，同时喂听读总数和 `noteListenTick`。约束和原生一致：页面可见 + 在播 + 单章上限由 store 自己卡；拖动进度条产生的大跳（delta ≥ 2 秒）不计。**顺带修好的不只是 XP**——网页端探索页那个「累计听读时长」对纯网页用户此前永远是 0。
- **飘字 / 获得提示 → 已做**：`components/achievements/AchievementFeedbackLayer.tsx` 挂在 `app/layout.tsx`，+XP 走 1.4 秒飘字，勋章 / 印章 / 升级走 2.6 秒横幅，依次播完。实测一次全新账本进阅读页依次弹出「+150 XP」→「获得勋章 · 初次翻开」→「升级 · Lv.2 初读」。
- **build 默认堆 OOM → 已做**：`package.json` 的 build 脚本写死 `NODE_OPTIONS=--max-old-space-size=8192`。
- **逐节微反馈 XP → 决定不做**：`noteVersesRead` 保留但不接线。网页阅读器没有现成的逐节可见性观察，要新加 IntersectionObserver，改动面大于收益；等网页端真有活跃阅读用户再说。

### 仍然开着

#### 1. 网页端的听读计时没有在浏览器里实测

- **现状**：代码已接线、`tsc` / `lint` 通过，但**没跑通一次真实播放**——开发环境里 `/read/<书>/<章>` 页面上找不到经文朗读的入口按钮（首页底栏有 `play_arrow`，章节页没有；`translationSupportsChapterAudio` 对和合本是 true，所以不是译本不支持）。没有播放就触发不了 `playScriptureChapter`，也就验不了那段累加。
- **影响**：如果网页端的经文朗读入口本来就只在某些壳层状态下出现，这段计时可能长期不触发，等于白写；也可能一切正常，只是我没找到入口。
- **需要 Josh 决定**：告诉我网页端从哪儿点开经文朗读（或者确认网页端目前**就是没有**章节朗读入口）。**我的推荐**：下一轮先花五分钟确认这个入口的现状——如果网页端确实没有经文朗读，那第一条「已做」的实际价值只剩「把总数的变更通知修好」，听读 XP 要等朗读入口先做出来。

#### 2. 换帐号 / 登出不清本机数据 —— 决定**维持现状**，但记下来

- **决定**：不在登出时清成就。网页端的 `logout`（`components/auth/AskbibleUserProvider.tsx`）是**故意**保留本机数据的：先 `flushMemberReadingSyncWebNow` 推一次，再 `markMemberReadingSyncPullOnlyWeb()` 标记成只拉不推，然后才登出。26 个 blob 全部如此。单把成就清掉会让它成为**唯一一个行为不同的 blob**，比现状更难理解。
- **仍然存在的问题**：共用一台电脑时，前一个人的本机进度会在下一个人登录后被合并进新帐号——这对**所有** blob 都成立，不是成就引入的。
- **需要 Josh 决定**：要不要单开一轮做「网页端登出清本机」。`clearAchievementsForAccountSwitch()` 已经导出、就位，等那一轮一起接。**我的推荐**：值得做，但要整块做（26 个 blob 一起），不要零敲碎打。


## 配色系统整改（2026-09-19）

完整方案见 [`docs/design-color-system.md`](design-color-system.md)，已落地的部分记在
[`docs/DECISIONS.md`](DECISIONS.md)。

**已做**：四色板换新 + 旧色迁移 + 颜料叠 alpha（四端）；色板按钮改合成色；
`humanSpeech` 深色修正；跟读高亮改成按句、贴字铺（四端）；网页 / RN 跟读高亮重开；
三种绿收敛到 `READ_DONE_ACCENT #65775C`。

**还没做，需要 Josh 决定：**

1. **语义色 token 要不要三端共享**（推荐：晚点做，但规则已经先锁上了）。
   成功 / 提醒 / 危险 / 禁用四档，表在 design-color-system.md 第四节 ——
   2026-09-20 已把那一节改成**硬规则**：只准四档，要加第五个先改文档再改代码，
   开关 on 态 / 选中态一律用 accent 而不是成功绿，中性徽章不给新颜色。
   **规则先落盘，是因为真正会复发的不是主色，是后面新功能随手加的第四第五个语义色**
   （`#7BC96F` / `#0FBCDB` / `#F48FB1` / `#34C759` 当初就是这么进来的）。
   还没做的是把四档抽成三端共享的 token 模块 —— 那是结构性改动，现在各端各写各的，
   改一次要动四处。
2. **跟读高亮的浓淡**：现在跟读黄 alpha 是 .55（浅）/ .34（深），比划重点的灯油黄 .45 重一点，
   为的是和「我自己划的」分开。真机上看如果觉得还是太抢，告诉我调到 .45 或更低。
3. **跟读时自动滚到屏幕中间**：当初和跟读高亮一起被摘掉的还有这一条（滚屏抢手势）。
   这次只重开了高亮，滚屏没碰。要不要一起开？

**不属于本项目**：成就详情弹窗关闭 × 点不中（SF Symbol 缺 `contentShape`）——
那个弹窗是「听到」（03MyClass）的界面，不在 01AskBible 里，要修得去那个项目。

### 已关闭

- 2026-09-19 四色板换不换 → 换了，带迁移表。
- 2026-09-19 改动范围做到第几步 → Josh「你决定你处理」，1–4 步全做。
- 2026-09-19 三种绿要不要收敛 → 收了，统一 `#65775C`。
- 2026-09-19 网页 / RN 跟读要不要重开 → 开了，并且直接是「按句」。

## 会员同步死循环修完之后（2026-09-19）

根因与实测数据见 [`docs/DECISIONS.md`](DECISIONS.md) 同日那节。iOS / 安卓两端已修、已装真机验证、已推 main（`33294b93`）。

**还没做，需要 Josh 决定：**

1. **iOS 冷启动耗时要不要再优化**（推荐：先看一眼数字再定）。
   - **现状**：修复前手机上的性能 HUD 报 **3376 ms**。那次测量正赶上死循环占死 MainActor，
     首帧被硬生生推迟，所以这个数字**不能代表修好后的真实启动耗时**。同一套业务逻辑在安卓上
     冷启动是 784～1174 ms（`am start -W` 实测三次），属于正常水平。iOS 启动路径也静态查过：
     `ScriptureStore.init` 只读 UserDefaults，首页金句 manifest 220 KB 解析是毫秒级，
     没有别的同步重活。
   - **影响**：如果新数字已经掉到 1 秒上下，就不必动；真要再压，得改 `RootView` 里
     12 个 `@StateObject` 的构造时机（首帧前全建），那是有风险的改动，值得单开一轮用
     Instruments 先量出谁占大头，不该凭猜改。
   - **需要 Josh 决定**：打开 App 看一眼性能 HUD 的新数字告诉我。**我的推荐**：≤1.5 秒就收工；
     还在 3 秒上下，我单开一轮做懒加载。

2. **同类写法要不要全库扫一遍**（推荐：值得，但单开一轮）。
   - **现状**：这次的坑是「`inFlight` 这类『谁在跑』的句柄，清理挂在**另一条** Task/协程上，
     等待方却假设 await 返回时它已经被清了」。只在 `MemberReadingSyncEngine` 上确认并修了
     iOS / 安卓两处；仓库里别的地方有没有同样的模式，**没有系统扫过**。
   - **影响**：同一个写法在别处就是同一颗雷 —— 表现同样是「没有崩溃报告的秒退」，
     极难查（这次是靠 Jetsam 的 `per-process-limit` + `malloc_history` 才定位到行）。
   - **需要 Josh 决定**：要不要单开一轮，按「`Task {` / `scope.launch {` 里清某个共享句柄」
     这个模式全库扫一遍。**我的推荐**：做，但别和别的活混在一轮里。

### 已关闭

- 2026-09-19 真机「启动几秒就关了」是设备问题还是代码问题 → 代码问题，`flushNow` 递归空转，已修并真机验证。
- 2026-09-19 屏幕顶部那个 `AskBible 3376 ms` 白胶囊是什么 → iPhone 系统的性能 HUD（设置 → 开发者），不是 App 画的。


## 安卓发版渠道：Play 版不能引导用户去下载页（2026-09-23）

- **背景**：Josh 问「Play 包也可以推这个版本吗，或者我们把它放在里面，用户点后，自己先下一次我们的版」。
- **答案是不行**，两条都踩 Google Play 政策红线：
  1. Play 版 App **不能自带下载 / 安装 APK 的逻辑**（Device and Network Abuse），
     所以 `release` 变体的 `SELF_UPDATE` 必须保持 false；
  2. Play 版**也不能引导用户去站外下载同一个 App**（Deceptive Behavior / 分发政策），
     加一个「去下载我们自己的版本」按钮同样会被拒审甚至下架。
- **正路**：同一个版本号打成 AAB 传 Play，让 Play 自己推更新给商店用户。
  脚本已有：`scripts/build-android-aab-local.sh` + `scripts/submit-android-aab-play.sh`。
- **已传**：1.0.44 (241) 的 AAB 已上 Play 的 **internal 轨道**（2026-09-23）。
  **提到 production 的命令这个会话跑不了**（被安全策略判成生产部署），要 Josh 自己跑：
  `node scripts/promote-android-play-track.mjs 241 production`
- **迁移提示**：Josh 要的「Play 版引导用户换到站外版」已实现但默认关，
  开关和风险见 `docs/DECISIONS.md` 同日那节。

## 三条老待办：2026-09-23 全部做完

Josh 2026-09-23「老的待办做掉」。三条都落地并各端编译通过、已装两台真机。

- **连续天数有反馈了**（三端）：续上第 2 天起，当天第一次刷新飘一条「连续 N 天」+ 小钵，
  一天只发一次；**中断时故意什么都不发**。不弹卡片。
- **今日计划完成有反馈了**（补原生）：网页端本来就有庆祝层 + `today-plan-complete.mp3`，
  iOS / 安卓缺；现在 `ReadingPlanStore.isTodayPlanComplete` + `PlanDayDone` 事件补齐。
- **「读完一章」有专属动效了**（三端）：章标题和细线短暂亮金再落回静止，不弹卡片。
  **挂在标题而不是新加一条阅读进度线** —— 后者要改读经页布局，收益不值，这条决定别再翻。

### 已关闭
- 2026-09-23 三条成就反馈待办 → 全部做完，见 `docs/DECISIONS.md` 同日那节。

### 【已关闭 2026-09-23】没有授权的 YouVersion 译本还在抓网页

- **和合本那 4 个**：Josh「1 改」→ 其中 3 个已改读本地库
  （见 `docs/DECISIONS.md`「和合本改读本地库」）。
  **例外 `cunp-zh-hant-god`（上帝版，414）**：本地没有上帝版，维持原样。
  不要用「神→上帝」替换去凑，「假神」「别神」这些会被改错。
- **另外 6 个**（新译本、和修、恢复本、mandarin）：Josh 2026-09-23
  「2 没关系的，那个是有授权的」→ **有授权，维持现状，不关闭。**

### 授权登记表还有格子要 Josh 填（2026-09-23）

- **现状**：`docs/授权登记.md` 已建（Josh「授权了」）。公有领域那一类填全了；
  版权译本那一类**只确认了「有授权」这件事**，授权方 / 授权类型 / 日期 / 授权文件位置都还空着。
- **影响**：最要紧的是**新译本、和修、恢复本、mandarin 这 4 个**——
  它们取数走的是抓 bible.com 页面，**代码上看不出任何授权痕迹**，
  所以下一个看代码的人（或下一个 Claude）还是会怀疑，还会再问一遍。
- **需要 Josh 决定**：把那 4 个的**授权方**和**授权文件存在哪**说一遍，我填进表里。
  其余格子（日期、授权类型）不急。

### YouVersion 授权目录怎么重新生成（2026-09-23）

`lib/bible/providers/youversion.ts` 里的 `YOUVERSION_LICENSED_VERSION_IDS` 是硬编码的快照。
授权档位变了、或者 YouVersion 加减译本，照下面重拉（**`language_ranges[]` 必填**）：

```bash
set -a; . ./.env.local; set +a
for L in eng zho; do
  curl -s -H "X-YVP-App-Key: $YVP_APP_KEY" \
    --get --data-urlencode "language_ranges[]=$L" \
    https://api.youversion.com/v1/bibles \
  | python3 -c "import sys,json;print([r['id'] for r in json.load(sys.stdin)['data']])"
done
```

## Play 测试轨道还挂着带全屏意图权限的旧包（2026-09-26）

- 现状：Play 2026-08-25 以「全屏意图权限政策」拒了更新。正式版已换成不带该权限的 1.0.46 (243)，
  但 alpha / beta 仍是 RN 的 127、TAIWAN 仍是 3、「623」轨道有个 60 的草稿，这些旧包大概率带
  `USE_FULL_SCREEN_INTENT`（expo-notifications 默认会加）。Claude 用 API 改轨道时被本机权限拦下，没改成。
- 影响：旧包不清掉，政策问题不会关闭，后续更新可能继续被拒。
- 需要 Josh 决定：推荐二选一——
  1. 放行 Claude 跑一次 API：把 internal / alpha / beta / TAIWAN 都设成 243，清空 623 草稿（一次 edit 提交，一次审核）；
  2. 自己在 Play 管理中心操作：测试与发布 → 每条测试轨道「建立新版本」→ 从 App Bundle 库加 243 → 发布；623 的草稿点「捨棄」。
  完成后到「政策审查状态」看这条是否变成已解决；必要时在问题详情页点「提出申诉 / 重新提交」。
- 另：Play 首页横幅提醒 **2026-09-30 前**要在「Android 开发人员验证」页登记在 Play 以外分发的包名和签名金钥
  （我们的下载页 APK 就是站外分发）。没登记的话，9-30 之后部分国家/地区经认证装置上可能装不了下载页的 APK。
