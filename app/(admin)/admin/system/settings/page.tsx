import { AdminPlaceholder } from "@/components/admin/AdminPlaceholder";

export const metadata = { title: "全局设置" };

export default function AdminSystemSettingsPage() {
  return <AdminPlaceholder title="全局设置" hint="系统级选项将集中于此（占位）。" />;
}
