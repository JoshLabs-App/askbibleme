import { AdminPlaceholder } from "@/components/admin/AdminPlaceholder";

export const metadata = { title: "探索" };

export default function AdminExplorePage() {
  return <AdminPlaceholder title="探索管理" hint="探索模块的配置将在此展开（占位）。" />;
}
