---
name: design-default
description: 简写：设计-默认。日常改 UI（taste-skill v2）。用户 @设计-默认 或 /设计-默认 时使用。
disable-model-invocation: true
---

# 设计 · 默认（taste-skill）

读取并严格遵循：`/Users/joshua/.cursor/skills/taste-skill/SKILL.md`

同时遵循：`.cursor/skills/_shared/askbible-skill-bridge.md` 中的 AskBible 边界。

然后执行用户在本轮描述的任务（若未说明页面，先问改哪一块）。
