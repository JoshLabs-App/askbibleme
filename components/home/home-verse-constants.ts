import { HOME_VERSE_DEFAULT_STABLE_SEC } from "@/lib/home-prayer-pools/constants";

/** 首页中部轮播经文：默认约 7 秒一节，淡入淡出见 `HomeVerseRotator` */
export const HOME_VERSE_DEFAULT_STABLE_MS = HOME_VERSE_DEFAULT_STABLE_SEC * 1000;
export const HOME_VERSE_FADE_MS = 4_000;
/** 自然首页视频上经文：与 App `HomeVerseOverlay` 一致 */
export const NATURE_HOME_VERSE_FADE_MS = 800;
